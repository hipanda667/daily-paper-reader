---
title: "Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning"
title_zh: 简单方案行之有效：视觉-语言-动作模型是强化学习下的天然持续学习者
authors: "Jiaheng Hu, Jay Shim, Chen Tang, Yoonchang Sung, Bo Liu, Peter Stone, Roberto Martin-Martin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11653v1"
tags: ["query:vla-ar"]
score: 8.0
evidence: 使用LoRA对VLA模型进行持续强化学习
tldr: 本研究探讨了视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现，挑战了“顺序微调会导致灾难性遗忘”的传统观点。通过在三个模型和五个基准测试上的系统实验，作者发现简单的顺序微调结合低秩自适应（LoRA）表现出极强的鲁棒性，不仅具有高塑性且几乎无遗忘，甚至优于复杂的CRL方法。这一发现揭示了大模型、参数高效微调与在线强化学习的协同效应，为构建可自我进化的具身智能体提供了简洁高效的新路径。
selection_source: fresh_fetch
motivation: 旨在验证在VLA模型中，简单的顺序微调是否真的如传统持续学习理论所言会面临严重的灾难性遗忘。
method: 在多个VLA模型和复杂强化学习基准上，系统对比了结合LoRA的顺序微调与多种复杂持续学习策略的性能。
result: 实验证明结合LoRA的顺序微调在保持学习新任务能力的同时，能有效保留旧任务知识并维持强大的零样本泛化能力。
conclusion: 大规模预训练模型与参数高效微调及在线强化学习的结合，使得简单的顺序微调成为VLA持续学习中极具竞争力的方案。
---

## 摘要
针对视觉-语言-动作（VLA）模型的持续强化学习（CRL）是实现能够在开放、演进的环境中进行自适应的自我提升具身智能体的一个极具前景的方向。然而，持续学习的传统观点认为，朴素的顺序微调（Seq. FT）会导致灾难性遗忘，因此需要复杂的 CRL 策略。在这项工作中，我们退后一步，对三种模型和五个具有挑战性的终身强化学习基准上的大规模预训练 VLA 的 CRL 进行了系统研究。我们发现，与既有观念相反，结合低秩自适应（LoRA）的简单顺序微调表现异常强劲：它实现了高可塑性，几乎没有表现出遗忘，并保持了强大的零样本泛化能力，经常优于更复杂的 CRL 方法。通过详细分析，我们表明这种鲁棒性源于大规模预训练模型、参数高效自适应和同策略（on-policy）强化学习之间的协同作用。这些组件共同重塑了稳定性-可塑性权衡，使持续自适应既稳定又具扩展性。我们的结果将顺序微调定位为 VLA 持续强化学习的一种强大方法，并为大模型时代的终身学习提供了新的见解。代码可在 github.com/UT-Austin-RobIn/continual-vla-rl 获取。

## Abstract
Continual Reinforcement Learning (CRL) for Vision-Language-Action (VLA) models is a promising direction toward self-improving embodied agents that can adapt in openended, evolving environments. However, conventional wisdom from continual learning suggests that naive Sequential Fine-Tuning (Seq. FT) leads to catastrophic forgetting, necessitating complex CRL strategies. In this work, we take a step back and conduct a systematic study of CRL for large pretrained VLAs across three models and five challenging lifelong RL benchmarks. We find that, contrary to established belief, simple Seq. FT with low-rank adaptation (LoRA) is remarkably strong: it achieves high plasticity, exhibits little to no forgetting, and retains strong zero-shot generalization, frequently outperforming more sophisticated CRL methods. Through detailed analysis, we show that this robustness arises from a synergy between the large pretrained model, parameter-efficient adaptation, and on-policy RL. Together, these components reshape the stability-plasticity trade-off, making continual adaptation both stable and scalable. Our results position Sequential Fine-Tuning as a powerful method for continual RL with VLAs and provide new insights into lifelong learning in the large model era. Code is available at github.com/UT-Austin-RobIn/continual-vla-rl.

---

## 论文详细总结（自动生成）

这是一份关于论文《Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在具身智能领域，构建能够随时间不断学习新技能且不遗忘旧技能的“自进化”智能体是终极目标。传统的持续学习（Continual Learning）理论认为，简单的**顺序微调（Sequential Fine-Tuning, Seq. FT）**会导致严重的“灾难性遗忘”。
*   **核心问题**：在大规模预训练视觉-语言-动作（VLA）模型时代，配合参数高效微调（PEFT）和强化学习（RL），这种“灾难性遗忘”是否依然是不可逾越的障碍？复杂的持续强化学习（CRL）算法是否真的必要？

### 2. 论文提出的方法论
论文提出了一种极其简洁的方案，并证明其在 VLA 模型上表现优异：
*   **核心思想**：直接使用**顺序微调（Seq. FT）**，即在任务流中逐个训练新任务，不使用任何复杂的正则化、重放或参数隔离机制。
*   **关键技术细节**：
    *   **LoRA（低秩自适应）**：仅更新极少量的低秩矩阵参数，保持预训练骨干网络冻结。
    *   **GRPO（群体相对策略优化）**：采用一种稳定的在线（On-policy）强化学习算法进行后训练（Post-training），通过群体内的奖励对比来估计优势函数。
    *   **协同效应**：论文分析指出，**大模型的高维空间（提供零空间避免干扰）**、**LoRA 的结构约束（限制更新幅度）**与**在线 RL 的隐式正则化（仅在当前策略支持范围内更新）**三者结合，天然地抑制了遗忘。

### 3. 实验设计
*   **VLA 模型**：评估了三种主流模型：OpenVLA-OFT（带动作分块）、Pi-0（基于流匹配）、OpenVLA（自回归）。
*   **基准测试（Benchmarks）**：
    *   **LIBERO**：包括 Object（物体）、Spatial（空间）、Long-horizon（长程）三个子集。
    *   **RoboCasa**：复杂的厨房场景任务。
    *   **ManiSkill**：基于物理引擎的操纵任务。
*   **对比方法**：
    *   **基准线**：Sequential Fine-Tuning (Seq. FT), Multitask Oracle (多任务训练上限)。
    *   **经典 CRL 算法**：EWC (正则化), Expert Replay (专家重放), DER (暗经验重放), Dynamic Weight Expansion (参数隔离)。
    *   **大模型适配方法**：SLCA (层级学习率), RETAIN (权重合并)。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **基础设施**：提到实验构建在 RLinf 基础设施之上，并得到了德克萨斯大学奥斯汀分校（UT Austin）机器学习实验室的计算资源支持。考虑到 7B 参数规模的 VLA 模型进行强化学习训练，通常需要多节点 A100 或 H100 集群。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同架构的 VLA 模型和 5 个主流机器人基准。
    *   每项实验均使用 3 个独立随机种子取平均值。
    *   进行了**鲁棒性测试**：包括相机角度扰动、光照变化、机器人初始位置变化以及任务顺序重排。
    *   进行了**消融实验**：分别移除 LoRA、移除 RL（改用监督微调 SFT）、或更换为小型 CNN 模型。
*   **充分性评价**：实验设计非常全面且客观。通过跨模型、跨基准、跨扰动的对比，有力地证明了结论的普适性，而非特定环境下的巧合。

### 6. 论文的主要结论与发现
*   **简单方案胜出**：结合 LoRA 的顺序微调在几乎所有指标上都达到或超过了复杂的 CRL 算法，且在**零样本泛化（Zero-shot Generalization）**上表现尤为突出。
*   **几乎无遗忘**：在 VLA 模型中，Seq. FT 的负向迁移（遗忘）通常低于 2%，甚至在某些情况下出现了正向迁移。
*   **机制揭秘**：
    1.  **RL 的作用**：在线 RL 相比 SFT 更不容易遗忘，因为它不会强行将概率分配给原始分布之外的动作。
    2.  **大模型的作用**：参数量越大，任务间的梯度干扰越小（Fisher 信息矩阵分析证实了这一点）。
    3.  **LoRA 的作用**：防止了单层权重的剧烈结构变化。

### 7. 优点
*   **挑战直觉**：通过严谨的实验挑战了持续学习领域的长期假设，具有很强的启发性。
*   **实用性极高**：为工业界和研究者提供了一个极其简单、无需额外存储（如重放缓冲区）或复杂调参的 VLA 持续学习方案。
*   **深入的理论分析**：不仅展示了结果，还从信息论、高维几何和 Fisher 信息的角度解释了“为什么有效”。

### 8. 不足与局限
*   **任务流长度**：实验中的任务序列通常包含 4-5 个任务。在更长的时间跨度（如数十或数百个任务）下，这种稳定性是否能持续仍需验证。
*   **算力细节缺失**：未提供具体的训练开销数据，不便于其他研究者评估复现成本。
*   **应用限制**：目前的结论主要基于仿真环境。虽然做了鲁棒性扰动，但在真实世界中由于传感器噪声和物理不确定性，Seq. FT 的稳定性是否会下降尚待观察。

（完）
