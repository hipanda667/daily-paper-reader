---
title: "World2Act: Latent Action Post-Training via Skill-Compositional World Models"
title_zh: World2Act：通过技能组合世界模型进行潜空间动作后训练
authors: "An Dinh Vuong, Tuan Van Vo, Abdullah Sohail, Haoran Ding, Liang Ma, Xiaodan Liang, Anqing Duan, Ivan Laptev, Ian Reid"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10422v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 将VLA动作与世界模型潜变量对齐的后训练框架
tldr: World2Act 是一种针对视觉-语言-动作（VLA）策略的后训练框架，旨在解决世界模型在像素空间监督下对伪影敏感的问题。该方法通过对比匹配将 VLA 动作与世界模型的视频动力学潜变量直接对齐，并引入基于 LLM 的技能分解流水线，使模型能够处理变长任务。实验证明其在 RoboCasa 和 LIBERO 基准测试中达到 SOTA 水平，并显著提升了真实世界中具身智能体的泛化能力。
selection_source: fresh_fetch
motivation: 现有的基于世界模型的后训练方法依赖像素级监督，容易受生成伪影影响，且难以应对机器人执行时长不一的变长任务。
method: 提出 World2Act 框架，利用对比匹配将动作与潜空间动力学对齐，并结合 LLM 驱动的技能分解流水线构建组合式世界模型。
result: "在 RoboCasa 和 LIBERO 仿真基准上取得领先性能，并在真实世界任务中实现了 6.7% 的性能提升。"
conclusion: 通过在潜空间进行动作对齐和引入技能分解机制，World2Act 有效增强了具身智能体在复杂环境下的鲁棒性和泛化性。
---

## 摘要
世界模型 (WMs) 已成为后训练视觉-语言-动作 (VLA) 策略的一种极具前景的方法，旨在提高其在环境变化下的鲁棒性和泛化能力。然而，大多数基于世界模型的后训练方法依赖于像素空间监督，这使得策略对来自不完美世界模型推演 (rollouts) 的像素级伪影和幻觉非常敏感。我们提出了 World2Act，这是一个后训练框架，它使用对比匹配目标将 VLA 动作直接与世界模型的视频动力学潜变量对齐，从而减少了对像素的依赖。后训练性能与推演质量紧密相关，但当前的世界模型在生成任意长度视频方面面临挑战，因为它们大多是在固定长度的片段上训练的，而机器人的执行时长差异很大。为了解决这个问题，我们提出了一种基于大语言模型 (LLM) 的自动技能分解流水线，将高层指令分割为底层提示词。我们的流水线生成了 RoboCasa-Skill 和 LIBERO-Skill，支持技能组合式世界模型在不同任务跨度下保持时间一致性。实验表明，将 World2Act 应用于 GR00T-N1.6 和 Cosmos Policy 等 VLA 模型，在 RoboCasa 和 LIBERO 上取得了当前最先进的结果，并将真实世界性能提升了 6.7%，增强了具身智能体的泛化能力。

## Abstract
World Models (WMs) have emerged as a promising approach for post-training Vision-Language-Action (VLA) policies to improve robustness and generalization under environmental changes. However, most WM-based post-training methods rely on pixel-space supervision, making policies sensitive to pixel-level artifacts and hallucination from imperfect WM rollouts. We introduce World2Act, a post-training framework that aligns VLA actions directly with WM video-dynamics latents using a contrastive matching objective, reducing dependence on pixels. Post-training performance is tied to rollout quality, yet current WMs struggle with arbitrary-length video generation as they are mostly trained on fixed-length clips while robotic execution durations vary widely. To address this, we propose an automatic LLM-based skill-decomposition pipeline that segments high-level instructions into low-level prompts. Our pipeline produces RoboCasa-Skill and LIBERO-Skill, supporting skill-compositional WMs that remain temporally consistent across diverse task horizons. Empirically, applying World2Act to VLAs like GR00T-N1.6 and Cosmos Policy achieves state-of-the-art results on RoboCasa and LIBERO, and improves real-world performance by 6.7%, enhancing embodied agent generalization.

---

## 论文详细总结（自动生成）

这是一份关于论文《World2Act: Latent Action Post-Training via Skill-Compositional World Models》的结构化深度总结：

### 1. 核心问题与整体含义
具身智能中的视觉-语言-动作（VLA）策略在面对环境变化或未见过的接触条件时，往往缺乏鲁棒性。虽然**世界模型（World Models, WM）**能提供物理动力学先验，但现有的后训练方法存在两大痛点：
*   **像素依赖性：** 现有方法多在像素空间进行监督（如通过逆动力学模型预测动作），这使得策略极易受到世界模型生成的伪影、幻觉和噪声的影响。
*   **变长生成难题：** 机器人任务时长跨度极大，而主流视频生成模型多在固定长度片段上训练，导致长程任务生成不稳定。

**World2Act** 的核心含义在于：通过在**潜空间（Latent Space）**而非像素空间对齐动作与动力学，并结合**技能分解**机制，实现更稳定、更具泛化性的 VLA 后训练。

---

### 2. 方法论：核心思想与技术细节
该方法分为两个关键组成部分：**技能组合式世界模型**和**潜空间动作对齐**。

#### A. 技能组合式世界模型（Skill-Compositional WM）
*   **自动分解流水线：** 利用 LLM（如 DeepSeek）将高层指令（如“设置咖啡机”）分解为有序的原子技能序列（如“拿起杯子”、“放到托盘”）。
*   **数据对齐：** 根据夹爪状态变化自动分割专家演示视频，构建了 **RoboCasa-Skill** 和 **LIBERO-Skill** 数据集，使视频长度分布更集中，降低生成难度。
*   **组合推理：** 采用自回归方式，将前一个技能视频的最后一帧作为下一个技能的初始条件，从而生成连贯的长程视频。

#### B. World2Act 后训练框架（两阶段）
*   **第一阶段：潜空间对齐（Latent Alignment）**
    *   训练**视频适配器（Video Adapter）**和**动作适配器（Action Adapter）**。
    *   使用**双向 InfoNCE 对齐损失**（对比学习）和动作重建损失（MSE），将视频动力学特征与机器人动作特征映射到共享的潜空间。
*   **第二阶段：残差策略后训练（Residual Policy Post-training）**
    *   冻结基础 VLA 模型，引入轻量级的**残差策略（Residual Policy）**。
    *   **核心公式/流程：** 在线推演时，将 VLA 输出的动作与残差修正结合，通过对比损失函数，使最终动作的潜空间表示向世界模型预测的视频动力学潜变量靠拢。

---

### 3. 实验设计
*   **数据集/场景：**
    *   **RoboCasa：** 复杂的厨房环境操作任务。
    *   **LIBERO：** 包含空间、物体、目标和长程任务的基准测试。
    *   **真实世界：** 使用 Franka Research 3 机械臂进行“抓取并放置杯子”、“关闭抽屉”等任务。
*   **对比方法（Baselines）：**
    *   **基础 VLA：** π0, π0.5, Cosmos Policy, GR00T-N1.5/N1.6, OpenVLA 等。
    *   **后训练基准：** DreamGen（基于像素空间）、VLA-RFT、Ctrl-World。

---

### 4. 资源与算力
论文明确记录了实验所需的算力资源：
*   **世界模型训练：** 使用 **8 台 AMD Instinct MI210 GPU (48GB)** 进行视频扩散模型的微调（约 10K 步）。
*   **后训练与推理：** 在单张 **NVIDIA RTX 4090 (24GB)** 上完成。
*   **训练时长：** 相比于 LoRA 微调（约 15.3 小时），World2Act 的残差策略训练仅需 **6.8 小时**，实现了 2.25 倍的加速。

---

### 5. 实验数量与充分性
实验设计非常详尽且具有客观性：
*   **多维度评估：** 涵盖了成功率（SR）、指令遵循度（IF）和物理对齐度（PA）。
*   **消融实验：** 验证了残差策略 vs LoRA、不同对比损失函数、以及技能分解对模型收敛速度的影响。
*   **泛化性测试：** 专门设计了“见过的任务”与“未见过的任务”分割实验，验证跨任务泛化能力。
*   **重复性：** 每个实验运行 5 个随机种子，每种子 50 次试验，确保了结果的统计学意义。

---

### 6. 主要结论与发现
*   **性能提升：** World2Act 在 RoboCasa 和 LIBERO 上均刷新了 SOTA。在 GR00T-N1.6 基础上，后训练使成功率从 70.1% 提升至 72.6%。
*   **潜空间优势：** 实验证明潜空间对齐比像素空间监督（如 DreamGen）更稳定，能有效过滤生成视频中的视觉伪影。
*   **技能分解的必要性：** 技能分解显著提高了世界模型的生成质量，并加快了后训练的收敛速度。
*   **真实世界有效性：** 在物理机器人上实现了 6.7% 的平均性能提升，证明了模拟到现实（Sim-to-Real）的可行性。

---

### 7. 优点
*   **创新性对齐：** 避开了脆弱的像素级逆动力学推导，利用 WM 的内部潜变量作为监督信号，极具鲁棒性。
*   **高效性：** 残差策略设计使得后训练不需要对庞大的 VLA 主干进行全量微调，节省了显存和时间。
*   **数据贡献：** 发布的 RoboCasa-Skill 和 LIBERO-Skill 为社区研究长程任务分解提供了宝贵资源。

---

### 8. 不足与局限
*   **架构敏感性：** 实验发现 World2Act 对不同 VLA 主干的提升幅度不一（如对 Cosmos Policy 的提升较小），这可能与主干网络本身的潜空间表征能力有关。
*   **物理精度限制：** 尽管引入了 WM 先验，但在涉及极高精度接触（如微小的旋钮操作）时，视觉想象与实际电机控制之间仍存在鸿沟。
*   **实时性挑战：** 虽然残差网络很轻量，但生成视频潜变量的过程在实时控制循环中仍可能带来一定的延迟压力。

（完）
