---
title: "CORAL: Scalable Multi-Task Robot Learning via LoRA Experts"
title_zh: CORAL：通过 LoRA 专家实现的可扩展多任务机器人学习
authors: "Yuankai Luo, Woping Chen, Tong Liang, Zhenguo Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09298v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 带有LoRA专家的VLA骨干无关框架
tldr: CORAL是一个针对视觉-语言-动作（VLA）模型的多任务学习框架，旨在解决多任务联合微调时的任务干扰和存储开销问题。该方法通过冻结预训练骨干网络，并为每个任务分配独立的轻量化LoRA专家，利用动态路由引擎实现零开销的任务切换。CORAL不仅通过参数隔离消除了负迁移，还支持持续学习新任务而无灾难性遗忘。实验证明其在真实机器人和仿真环境中均优于联合训练，具有极高的扩展性。
selection_source: fresh_fetch
motivation: 解决多任务机器人学习中任务间梯度冲突导致的负迁移，以及为每个任务维护全量模型带来的存储和部署难题。
method: 冻结预训练VLA骨干网络，为每个任务分配独立的轻量化LoRA专家，并由CORAL管理器根据指令动态路由和切换专家。
result: 在真实双臂机器人及LIBERO等多个仿真基准测试中，CORAL显著优于联合训练，并能有效处理细粒度的指令歧义。
conclusion: CORAL通过参数隔离实现了无干扰的多任务学习，为构建可扩展且支持持续学习的机器人系统提供了实用方案。
---

## 摘要
在现实世界的机器人中部署视觉-语言-动作（VLA）模型暴露了一个核心的多任务学习挑战：如何协调多任务机器人学习中的任务干扰。当多个任务在单一阶段进行联合微调时，来自不同任务的梯度可能会发生冲突，导致负迁移并降低每个任务的性能。然而，为每个任务维护一个单独的完整检查点通常在存储和部署上是不可行的。为了解决这一困境，我们提出了 CORAL，这是一个与骨干网络和具身形式无关的框架，旨在减轻多任务干扰，同时保持对连续新任务流的自然扩展性。CORAL 冻结单个预训练的 VLA 骨干网络，并为每个任务附加一个轻量级的低秩自适应（LoRA）专家；在运行时，动态推理引擎（CORAL 管理器）将语言指令路由到相应的专家，并实时切换专家，且具有零推理开销。这种严格的参数隔离避免了复杂的门控网络，并从结构上防止了参数级的跨任务干扰；作为一项附加功能，它还允许按顺序引入新任务，而不会因灾难性遗忘导致参数覆盖。我们在现实世界的 Galaxea R1 双臂移动协作机器人和三个仿真基准测试（LIBERO、WidowX、Google Robot）上验证了 CORAL。结果表明，CORAL 克服了细粒度的指令歧义，并显著优于联合训练，为终身多任务机器人学习提供了一个实用且可扩展的系统。

## Abstract
Deploying Vision-Language-Action (VLA) models in real-world robotics exposes a core multi-task learning challenge: reconciling task interference in multi-task robotic learning. When multiple tasks are jointly fine-tuned in a single stage, gradients from different tasks can conflict, causing negative transfer and reducing per-task performance. Yet maintaining a separate full checkpoint per task is often storage- and deployment-prohibitive. To address this dilemma, we present CORAL, a backbone- and embodiment-agnostic framework designed primarily to mitigate multi-task interference while remaining naturally extensible to a continuous stream of new tasks. CORAL freezes a single pre-trained VLA backbone and attaches one lightweight Low-Rank Adaptation (LoRA) expert per task; at runtime, a dynamic inference engine (the CORAL Manager) routes language instructions to the appropriate expert and swaps experts on the fly with zero inference overhead. This strict parameter isolation avoids complex gating networks and prevents parameter-level cross-task interference by construction; as an added capability, it also enables sequentially introducing new tasks without parameter overwriting caused by catastrophic forgetting. We validate CORAL on a real-world Galaxea R1 dual-arm mobile manipulator and three simulation benchmarks (LIBERO, WidowX, Google Robot), where CORAL overcomes fine-grained instructional ambiguity and substantially outperforms joint training, yielding a practical and scalable system for lifelong multi-task robot learning. Website: https://frontierrobo.github.io/CORAL

---

## 论文详细总结（自动生成）

以下是对论文《CORAL: Scalable Multi-Task Robot Learning via LoRA Experts》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对视觉-语言-动作（VLA）模型在现实机器人部署中的三大核心挑战：
*   **多任务干扰（Negative Transfer）：** 在单一阶段联合微调多个任务时，不同任务的梯度冲突会导致性能下降，尤其是在指令细微差异导致歧义时。
*   **存储与部署成本：** 为每个任务维护一个完整的模型检查点（Full Checkpoint）在存储和边缘设备部署上是不可行的。
*   **灾难性遗忘：** 顺序学习新任务时，新参数会覆盖旧知识。
**CORAL** 的提出旨在通过“参数隔离”的思想，构建一个与骨干网络无关、可扩展且无推理开销的多任务学习框架。

### 2. 方法论
CORAL 采用两阶段学习范式，核心思想是将通用具身能力与特定任务控制解耦：
*   **具身感知通用预训练：** 首先在多样化数据集上训练/微调 VLA 骨干网络（$\theta_{base}$），使其掌握基本的运动学和视觉语义，随后永久冻结该骨干网络。
*   **轻量化任务特定 LoRA 专家：** 为每个任务 $k$ 训练一个独立的 LoRA 适配器（$\theta_k$）。LoRA 模块被注入到 VLM 编码器和动作头的注意力层中。
*   **确定性任务路由：** 不同于传统的混合专家模型（MoE）需要复杂的学习门控网络，CORAL 利用机器人任务的特性——**语言指令本身即索引**。系统直接根据指令确定性地路由到对应的 LoRA 专家。
*   **CORAL 管理器（动态切换）：** 在推理时，管理器负责在内存中动态加载、合并和卸载 LoRA 权重。通过将低秩矩阵直接合并进骨干权重，实现了**零额外推理开销（FLOPs 和延迟）**，切换过程在 100ms 内完成。

### 3. 实验设计
实验涵盖了仿真和真实世界两个维度，验证了框架的通用性：
*   **仿真基准测试：**
    *   **LIBERO：** 包含 40 个任务（Spatial, Object, Goal, Long 四个子集）。
    *   **WidowX (Simpler-Bridge)：** 评估高保真环境下的表现。
    *   **Google Robot (Simpler-Fractal)：** 测试鲁棒性和变体聚合得分。
*   **真实世界评估：**
    *   使用 **Galaxea R1 Lite** 双臂移动协作机器人。
    *   **跨场景零样本泛化：** 8 个复杂双臂协作任务（如翻书、插吸管、折 T 恤）。
    *   **新能力获取与遗忘分析：** 针对完全未见过的任务（开门、按电梯按钮）进行 8 种变体的学习。
*   **对比方法：** 包括 π0、SimVLA、X-VLA、RT-2-X 等 SOTA 模型，以及“独立全量微调”和“联合全量微调”两种基准策略。

### 4. 资源与算力
*   **模型规模：** 实验使用了 0.8B (SimVLA) 和 3B (π0.5) 两种规模的骨干网络。
*   **训练细节：** LoRA 专家训练极其轻量，仅需 1-5 个 Epoch（LIBERO 任务仅需 50 步优化）。
*   **存储开销：** 一个秩为 16 的专家仅占约 **26MB**，比全量模型缩小了约 100 倍。
*   **算力说明：** 论文未详细列出训练所用的具体 GPU 型号和总机时，但强调了其在边缘设备上的可行性，并提到专家切换在单 GPU 上小于 100ms。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 40 个仿真任务和 16 个真实世界任务变体，实验组数较多。
*   **充分性：** 论文不仅对比了成功率，还深入分析了存储效率、推理延迟和抗遗忘能力。
*   **客观性：** 通过在不同骨干网络（SimVLA 和 π0.5）上应用 CORAL，证明了其“骨干无关”的特性；对比实验中保持了超参数的一致性，具有较好的公平性。

### 6. 主要结论与发现
*   **性能领先：** CORAL 在 LIBERO 基准上达到了 **99.3%** 的平均成功率，刷新了纪录。
*   **解决干扰：** 在新能力获取实验中，联合微调因任务干扰仅有 24.5% 的成功率，而 CORAL 达到了与独立全量微调相当的高水平（接近 100%）。
*   **高效扩展：** 证明了可以在不增加推理延迟的前提下，通过极小的存储代价（1GB 存 40 个专家）实现多任务扩展。
*   **零遗忘：** 由于参数严格隔离，顺序学习新任务对旧任务的成功率影响为零。

### 7. 优点
*   **系统架构优雅：** 利用语言指令作为天然路由器，规避了 MoE 中最难处理的门控负载均衡问题。
*   **部署友好：** 零推理开销和极低的存储需求，非常适合资源受限的移动机器人。
*   **通用性强：** 证明了在不同机器人形态（单臂、双臂、移动底座）和不同 VLA 模型上均有效。

### 8. 不足与局限
*   **指令依赖：** 路由完全依赖语言指令，若用户指令存在极度歧义或未包含在路由映射中，可能导致错误的专家激活。
*   **任务粒度：** 目前专家是按任务定义的，当任务数量达到成千上万时，如何构建层级化的专家结构或共享相关任务知识仍有待探索。
*   **在线学习缺失：** 论文主要关注离线微调后的部署，尚未涉及机器人在交互过程中的实时在线 LoRA 更新。

（完）
