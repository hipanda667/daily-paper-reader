---
title: "FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model"
title_zh: FutureVLA：视觉-语言-动作模型的联合视觉运动预测
authors: "Xiaoxu Xu, Hao Li, Jinhui Ye, Yilun Chen, Jia Zeng, Xinyi Chen, Linning Xu, Dahua Lin, Weixin Li, Jiangmiao Pang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10712v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 用于VLA的联合视觉运动预测
tldr: 针对具身智能体在视觉感知与动作执行联合建模中的挑战，本文提出 FutureVLA。现有方法常受限于无关视觉细节或缺乏时间连续性，FutureVLA 通过联合视觉-动作预测架构，利用门控机制解耦视觉状态与动作建模，提取广义物理先验。在预训练阶段学习联合嵌入，并在后期通过潜在嵌入对齐技术增强下游 VLA 模型，无需改变推理架构即可显著提升模型性能。
selection_source: fresh_fetch
motivation: 现有VLA模型在联合建模视觉感知与动作执行时，容易受无关视觉细节干扰或缺乏时间连续性，难以有效捕捉紧密耦合的视觉-动作相互作用。
method: 提出一种联合视觉-动作预测架构，通过门控机制解耦视觉与动作信息，并利用潜在嵌入对齐策略将物理先验注入下游模型。
result: 实验结果表明，FutureVLA 在多种异构操作数据集上一致性地提升了现有 VLA 框架的性能和泛化能力。
conclusion: 通过解耦视觉状态与动作建模并联合编码物理先验，FutureVLA 为增强具身智能体的预测预见性提供了一种高效且通用的解决方案。
---

## 摘要
预测性预见对于智能具身智能体至关重要。由于机器人的运动执行本质上受其对环境几何视觉感知的约束，有效地预测未来需要捕捉这种紧密耦合的视觉运动相互作用。虽然最近的视觉-语言-动作（VLA）模型尝试引入未来引导，但它们在处理这种联合建模方面仍面临困难。现有的显式方法将模型容量分散到了与任务无关的视觉细节上，而依赖稀疏帧对的隐式方法则破坏了时间连续性。由于过度依赖视觉重建，这些方法变得由视觉主导，将静态场景上下文与动态动作意图纠缠在一起。我们认为，有效的联合视觉运动预测建模既需要时间连续性，也需要视觉条件下的监督解耦。为此，我们提出了 FutureVLA，其特点是采用了一种新颖的联合视觉运动预测架构。FutureVLA 旨在通过先解耦视觉和运动信息，然后联合编码广义物理先验来提取联合视觉运动嵌入。具体而言，在预训练阶段，我们利用异构操作数据集并引入联合视觉运动门控（Joint Visuomotor Gating）机制，从结构上将视觉状态保持与时间动作建模分离。它允许运动流专注于连续的物理动力学，同时显式地查询视觉标记以获取环境约束，从而产生高度泛化性的联合视觉运动嵌入。随后，在后训练阶段，我们采用潜嵌入对齐策略，使各种下游 VLA 模型能够在不修改其推理架构的情况下内化这些时间先验。大量实验表明，FutureVLA 持续改进了 VLA 框架。

## Abstract
Predictive foresight is important to intelligent embodied agents. Since the motor execution of a robot is intrinsically constrained by its visual perception of environmental geometry, effectively anticipating the future requires capturing this tightly coupled visuomotor interplay. While recent vision-language-action models attempt to incorporate future guidance, they struggle with this joint modeling. Existing explicit methods divert capacity to task-irrelevant visual details, whereas implicit methods relying on sparse frame pairs disrupt temporal continuity. By heavily relying on visual reconstruction, these methods become visually dominated, entangling static scene context with dynamic action intent. We argue that effective joint visuomotor predictive modeling requires both temporal continuity and visually-conditioned supervision decoupling. To this end, we propose FutureVLA, featuring a novel Joint Visuomotor Predictive Architecture. FutureVLA is designed to extract joint visuomotor embeddings by first decoupling visual and motor information, and then jointly encoding generalized physical priors. Specifically, in the pretraining stage, we leverage heterogeneous manipulation datasets and introduce a Joint Visuomotor Gating mechanism to structurally separate visual state preservation from temporal action modeling. It allows the motor stream to focus on continuous physical dynamics while explicitly querying visual tokens for environmental constraints, yielding highly generalizable joint visuomotor embeddings. Subsequently, in the post-training stage, we employ a latent embeddings alignment strategy, enabling diverse downstream VLA models to internalize these temporal priors without modifying their inference architectures. Extensive experiments demonstrate that FutureVLA consistently improves VLA frameworks.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model》** 的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：具身智能体（Embodied Agents）如何有效地预测未来状态以指导当前动作。
*   **研究背景**：现有的视觉-语言-动作（VLA）模型在引入“未来引导”时存在两大缺陷：
    1.  **视觉主导（Visual Dominance）**：显式预测未来视频帧的方法过度关注与任务无关的背景细节，导致模型容量被浪费，且静态场景与动态动作意图纠缠不清。
    2.  **时间不连续（Temporal Discontinuity）**：隐式预测方法通常依赖稀疏的帧对（如仅起始帧和结束帧），破坏了动作执行的连续性，难以与多步动作块（Action Chunks）对齐。
*   **整体含义**：论文认为有效的预测建模需要**时间连续性**和**视觉条件下的监督解耦**，即视觉提供静态空间约束，动作捕捉连续动态演化。

### 2. 方法论：FutureVLA 架构
FutureVLA 采用两阶段训练范式，核心是**联合视觉运动预测架构（JVPA）**：

*   **第一阶段：联合视觉运动预训练（Joint Visuomotor Pretraining）**
    *   **视觉标记化**：使用预训练的 **3D-VAE**（来自 WAN 模型）将连续 17 帧视频剪辑编码为紧凑的时间标记，保留细粒度的时间结构。
    *   **联合视觉运动门控（Joint Visuomotor Gating, JVG）**：这是核心创新。将标记分为“视觉流”和“动作流”。
        *   **视觉流**：仅负责重建初始帧（$O_t$）的潜在嵌入，强制其保留环境几何约束。
        *   **动作流**：通过**门控交叉注意力机制**显式查询视觉流中的空间特征（Spatial Affordances），从而在不被视觉细节淹没的前提下，学习物理一致的动作动力学。
    *   **解耦监督**：视觉流受重建损失约束，动作流受动作回归（OFT 风格）或条件流匹配（GR00T 风格）损失约束。

*   **第二阶段：引导式 VLA 后训练（Guided VLA Post-training）**
    *   **潜在嵌入对齐**：将预训练得到的联合嵌入作为“语义锚点”。在训练下游 VLA 时，通过一个轻量级适配器，强制 VLA 的中间表示与这些具有未来感知能力的物理先验对齐。
    *   **推理优势**：在推理阶段，VLA 模型无需输入未来帧，仅凭当前观测即可利用内化的物理先验进行决策。

### 3. 实验设计
*   **数据集**：使用了包含 1560 万帧的混合数据集，涵盖 **Open X-Embodiment (OXE)**、**LIBERO**、**Bridge** 和 **Droid** 等。
*   **Benchmark（基准测试）**：
    1.  **SimplerEnv**：在 Google Robot 和 WidowX 机器人上评估（视觉匹配与变体聚合设置）。
    2.  **LIBERO**：评估 Object、Spatial、Goal 和 Long（长程任务）四个子集。
    3.  **LIBERO-Plus**：评估模型在光照、背景、相机视角等扰动下的鲁棒性。
    4.  **真实世界**：使用 Franka 机器人执行四个复杂任务：制作汉堡、插花、勺子舀豆子、擦白板。
*   **对比方法**：包括原始基准（OpenVLA, $\pi_0$, GR00T-N1.5）、显式引导方法（WorldVLA, DreamVLA）和隐式引导方法（UniVLA, Villa-X, LAPA）。

### 4. 资源与算力
*   **GPU 型号与数量**：使用了 **16 张 NVIDIA A100 GPU**。
*   **训练时长**：预训练阶段大约耗时 **3 天**。
*   **参数规模**：后训练阶段采用了 Qwen3-VL-4B-Instruct 作为 VLM 主干。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   **跨平台验证**：涵盖了 3 种不同的机器人平台（Google, WidowX, Franka）。
    *   **消融实验**：验证了 JVPM 引导的有效性、JVG 门控机制的作用、时间采样密度（2/5/9/17 帧对比）、时间窗口长度（9-21 帧）以及重建目标（首帧 vs 末帧）的影响。
    *   **鲁棒性测试**：在 LIBERO-Plus 上进行了超过 10,000 个任务的测试。
*   **客观性与公平性**：作者在“统一架构”下重新实现了对比方法（如 LAPA, Villa-X），排除了主干网络差异带来的干扰，实验设计非常严谨且具有说服力。

### 6. 主要结论与发现
*   **性能提升**：在 SimplerEnv 上平均提升 **11.4%**，在真实世界任务中相比 $\pi_0$ 提升了 **26.7%**。
*   **长程任务优势**：在 LIBERO-Long 任务中表现尤为突出，证明了连续时间建模对长序列决策的帮助。
*   **物理一致性**：通过定量分析证明，FutureVLA 学习到的潜在空间与实际动作的物理一致性（PAAC 指标）相关性最强，成功避免了“视觉退化”。
*   **解耦的重要性**：实验证明，简单地增加多帧输入而不进行视觉/动作解耦，反而可能导致性能下降。

### 7. 优点与亮点
*   **架构创新**：提出的 JVG 机制优雅地解决了视觉细节干扰动作建模的问题。
*   **时间连续性**：利用 3D-VAE 处理连续视频流，比传统的稀疏采样更符合机器人控制的本质。
*   **通用性**：该方法作为一种“插件式”的先验引导，可以提升不同架构（如基于回归的 OFT 或基于流匹配的 GR00T）的 VLA 模型。
*   **真实感**：真实世界实验涵盖了接触密集型任务（如擦白板），验证了模型在复杂物理交互中的可靠性。

### 8. 不足与局限
*   **传感器单一性**：目前主要依赖视觉感知。论文提到，对于擦白板等接触密集型任务，如果能引入触觉或力矩反馈，预测建模会更完善。
*   **计算开销**：虽然推理时不需要未来帧，但预训练阶段需要处理高分辨率的连续视频流，对算力和存储有一定要求。
*   **数据依赖**：性能高度依赖于高质量的多帧视频数据，对于缺乏连续轨迹的稀疏数据集可能效果受限。

（完）
