---
title: "Act, Think or Abstain: Complexity-Aware Adaptive Inference for Vision-Language-Action Models"
title_zh: 行动、思考或弃权：视觉-语言-动作模型的复杂度感知自适应推理
authors: "Riccardo Andrea Izzo, Gianluca Bardaro, Matteo Matteucci"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05147v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 针对VLA模型架构的复杂度感知自适应推理
tldr: 针对视觉-语言-动作（VLA）模型推理延迟高且缺乏不确定性估计的问题，本文提出一种受人类认知启发的自适应推理框架。该框架通过将视觉嵌入投影到集成评估器中，根据任务复杂度动态选择直接执行（Act）、推理（Think）或中止（Abstain）。实验表明，仅利用视觉特征即可高效识别任务复杂度，在显著减少计算开销的同时提升了系统在异常情况下的安全性。
selection_source: fresh_fetch
motivation: 现有的VLA模型无差别地应用复杂推理机制导致计算效率低下，且在面对分布外任务时缺乏防止灾难性失败的安全性评估。
method: 提出一种自适应框架，利用视觉-语言骨干网络的潜在嵌入构建参数化与非参数化评估器，根据感知状态的复杂度动态路由执行路径。
result: "在LIBERO基准和真实机器人测试中，仅需5%的训练数据，视觉配置即可达到80%的F1分数，证明了视觉特征在推断任务复杂度方面的优越性。"
conclusion: 该研究通过引入复杂度感知的自适应推理，成功实现了任务执行效率与安全性的平衡，为构建高效、可靠的具身智能系统提供了新方案。
---

## 摘要
当前关于视觉-语言-动作（VLA）模型的研究主要集中在通过既有的推理技术来增强泛化能力。虽然这些改进是有效的，但它们不可避免地增加了计算复杂度和推理延迟。此外，这些机制通常被无差别地应用，导致在简单任务上资源分配效率低下，同时又无法提供必要的确定性估计，以防止在分布外（OOD）任务上发生灾难性失败。受人类认知启发，我们提出了一个自适应框架，根据感知状态的复杂度动态路由 VLA 的执行。我们的方法通过将潜在嵌入投影到参数化和非参数化估计器的集成中，将 VLA 的视觉-语言主干转化为一种主动检测工具。这使得系统能够立即执行已知任务（Act），对模糊场景进行推理（Think），并在遇到显著的物理或语义异常时预先停止执行（Abstain）。在我们的实证分析中，我们观察到一种现象：由于语言的语义不变性，仅凭视觉嵌入在推断任务复杂度方面表现更优。在 LIBERO 和 LIBERO-PRO 基准测试以及真实机器人上的评估结果显示，我们的纯视觉配置仅使用 5% 的训练数据就达到了 80% 的 F1 分数，证明了其作为可靠且高效的任务复杂度检测器的地位。

## Abstract
Current research on Vision-Language-Action (VLA) models predominantly focuses on enhancing generalization through established reasoning techniques. While effective, these improvements invariably increase computational complexity and inference latency. Furthermore, these mechanisms are typically applied indiscriminately, resulting in the inefficient allocation of resources for trivial tasks while simultaneously failing to provide the uncertainty estimation necessary to prevent catastrophic failure on out-of-distribution tasks. Inspired by human cognition, we propose an adaptive framework that dynamically routes VLA execution based on the complexity of the perceived state. Our approach transforms the VLA's vision-language backbone into an active detection tool by projecting latent embeddings into an ensemble of parametric and non-parametric estimators. This allows the system to execute known tasks immediately (Act), reason about ambiguous scenarios (Think), and preemptively halt execution when encountering significant physical or semantic anomalies (Abstain). In our empirical analysis, we observe a phenomenon where visual embeddings alone are superior for inferring task complexity due to the semantic invariance of language. Evaluated on the LIBERO and LIBERO-PRO benchmarks as well as on a real robot, our vision-only configuration achieves 80% F1-Score using as little as 5% of training data, establishing itself as a reliable and efficient task complexity detector.

---

## 论文详细总结（自动生成）

这是一份关于论文《Act, Think or Abstain: Complexity-Aware Adaptive Inference for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前视觉-语言-动作（VLA）模型的研究主要集中在通过增加推理步骤（如思维链 CoT）来提升泛化能力。然而，这种做法存在两个显著缺陷：
*   **计算效率低下**：无论任务简单还是复杂，模型都无差别地执行高延迟的推理步骤，浪费计算资源。
*   **缺乏安全性保障**：模型在面对完全超出其能力范围（分布外，OOD）的任务时，往往会表现出“过度自信”，盲目执行可能导致物理损坏或灾难性失败。

受人类认知中“快思考与慢思考”以及“知难而退”的启发，本文提出了一个**复杂度感知的自适应推理框架**，旨在让 VLA 模型能够根据环境状态动态选择执行策略：直接行动（Act）、深入思考（Think）或放弃执行（Abstain）。

### 2. 方法论：核心思想与技术细节
该框架将 VLA 的骨干网络（Backbone）从被动的特征提取器转变为主动的复杂度检测器。

*   **特征提取**：从预训练的 VLM（如 SmolVLM-2）中提取视觉嵌入（$z_{vis}$）、文本嵌入（$z_{text}$）和融合嵌入（$z_{fused}$）。为了保证纯粹性，提取文本特征时不进行视觉调节。
*   **分布拟合与评分**：
    *   **PCA 降维**：将高维特征投影到 64 维空间，保留 95% 的方差。
    *   **参数化估计（GMM）**：使用高斯混合模型建模训练分布，通过马氏距离（Mahalanobis distance）衡量样本的新颖性。引入 **Ledoit-Wolf 收缩估计器**处理协方差矩阵奇异性问题。
    *   **非参数化估计（kNN）**：计算样本与训练集最近邻的欧氏距离，以捕捉局部几何密度。
*   **自适应路由（MLP 分类器）**：
    *   将 GMM 和 kNN 的评分汇总为向量，输入一个轻量级的 MLP。
    *   **Act**：识别为 ID（分布内），直接调用 VLA 生成动作。
    *   **Think**：识别为部分 OOD，触发 VLM 进行场景线索提取和子目标推理，增强上下文后再执行。
    *   **Abstain**：识别为完全 OOD，立即停止以确保安全。
*   **训练策略**：使用 **Mixup 策略**在 ID 和 OOD 特征之间进行插值，生成合成的“中间态”特征，帮助 MLP 学习“Think”路径的决策边界。

### 3. 实验设计
*   **数据集与基准**：
    *   **ID（分布内）**：LIBERO 数据集（模型已在此微调）。
    *   **部分 OOD**：LIBERO-PRO，包含物体、位置、语义、任务和环境的扰动。
    *   **完全 OOD**：lerobot 提供的 Franka 机器人操作数据集（不同的视角、光照和物体集）。
*   **对比方法**：
    *   **Baseline MLP**：直接在原始嵌入上训练的分类器。
    *   **模态消融**：仅视觉、仅文本、融合特征的各种组合。
    *   **组件消融**：GMM 与 kNN 的不同组合，以及 GMM 组件数 $k$ 的影响。
*   **测试场景**：仿真环境（LIBERO 系列）和真实机器人（SO-ARM 101）。

### 4. 资源与算力
*   **硬件**：推理时间测量使用了 **NVIDIA RTX Quadro 6000** GPU。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了该框架的轻量性。MLP 仅包含两个隐藏层（64 和 32 单元），且仅需 5% 的训练数据（约 800 个样本）即可达到近乎峰值的性能，表明其训练成本极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个 LIBERO 任务套件（Goal, Object, Spatial, Long）及其多种变体，每种变体进行 30 次试验（3 个随机种子）。
*   **消融实验**：针对数据规模（0.1% 到 25%）、GMM 组件数（1 到 10）、特征模态进行了详尽的对比。
*   **客观性**：通过混淆矩阵直观展示了不同配置下的误报率和漏报率，并对比了推理延迟（$T_{inf}$）和预防失败次数（PF），实验设计较为全面且公平。

### 6. 主要结论与发现
*   **视觉特征的优越性**：在推断物理任务复杂度时，**仅使用视觉嵌入**的效果显著优于文本或融合特征。这是因为语言具有“语义不变性”，无法捕捉物理环境中的细微异常。
*   **数据效率极高**：仅需 5% 的训练数据，视觉 GMM 配置即可达到 80% 以上的 F1 分数。
*   **安全性与效率双赢**：在完全 OOD 任务中，系统实现了近乎 100% 的拒绝率，将无效执行时间从 150 秒以上缩短至 3 秒左右；同时，“Think”路径在模糊场景下提升了约 6.67% 的成功率。

### 7. 优点
*   **自适应平衡**：成功解决了 VLA 模型在泛化性、实时响应和安全性之间的权衡问题。
*   **模型无关性**：该框架不依赖特定的 VLA 架构，具有很强的通用性。
*   **低开销**：引入的复杂度检测模块计算量极小，且能通过提前终止 OOD 任务大幅节省总推理时间。

### 8. 不足与局限
*   **硬边界问题**：将 Act/Think/Abstain 视为离散分类问题，可能导致在分布边缘出现决策抖动。
*   **模型覆盖有限**：实验主要基于 SmolVLA，尚未在 π0 或 OpenVLA 等更大规模的模型上进行广泛验证。
*   **依赖 ID 集合**：框架仍需要已知的分布内数据集来拟合密度估计器，对于完全零样本（Zero-shot）的场景支持有限。

（完）
