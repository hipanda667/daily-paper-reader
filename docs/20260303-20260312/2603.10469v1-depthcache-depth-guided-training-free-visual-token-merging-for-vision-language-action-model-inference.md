---
title: "DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference"
title_zh: DepthCache：用于视觉-语言-动作模型推理的深度引导无需训练视觉 Token 合并方法
authors: "Yuquan Li, Lianjie Ma, Han Ding, Lijun Zhu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10469v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 用于VLA推理的深度引导视觉标记合并
tldr: 针对视觉-语言-动作（VLA）模型推理延迟高的问题，本文提出 DepthCache 框架。该框架无需训练，利用深度信息作为结构先验，对视觉 Token 进行差异化合并：保留近场工作区细节，压缩远场背景。同时，它结合时间冗余和运动自适应策略优化多帧及辅助视图处理。实验表明，在保持极高成功率的同时，显著提升了推理速度和闭环控制响应。
selection_source: fresh_fetch
motivation: 现有的视觉 Token 压缩方法因均匀处理而破坏了机器人控制所需的空间推理能力，导致 VLA 模型推理效率与性能难以兼得。
method: 提出一种基于深度的 Token 合并策略，通过空间差异化压缩、跨帧分布式合并及运动自适应流水线，在不修改模型的情况下减少计算量。
result: "在 LIBERO 基准测试中，DepthCache 在三种主流 VLA 模型上实现了高达 1.28 倍的加速，且成功率下降不到 1%，远优于传统基准方法。"
conclusion: DepthCache 证明了利用深度和时间先验可以在不损失精度的前提下有效提升 VLA 模型的推理效率，适用于实时机器人控制场景。
---

## 摘要
视觉-语言-动作（VLA）模型实现了通用的机器人操作，但面临高推理延迟的问题。这一瓶颈源于大型语言骨干网络处理的海量视觉 Token。现有方法通常采用均匀的剪枝或合并 Token 策略，这会损害机器人控制中至关重要的空间推理能力。我们提出了 DepthCache，这是一个无需训练的框架，利用深度信息作为视觉 Token 压缩的结构先验。它将观测结果划分为基于深度的区域，并应用空间差异化的合并比例，在压缩远景背景的同时保留近场工作空间。为了利用时间冗余，DepthCache 将合并过程分布在连续帧中，在减少每步计算量的同时确保表示的一致性。运动自适应流水线根据末端执行器的动力学进一步优化了辅助视角的压缩。该框架无需修改模型，可推广到多种 VLA 架构。在 LIBERO 基准测试中，DepthCache 在三种 VLA 模型（pi_0.5、OpenVLA、GR00T）上实现了高达 1.28 倍的推理加速，且平均成功率下降不到 1%，而剪枝和合并基准在同等压缩率下会导致 4-24% 的性能下降。在物理机械臂上的真实世界实验表明，DepthCache 在延迟敏感的场景中实现了更快的任务吞吐量和更灵敏的闭环控制。

## Abstract
Vision-Language-Action (VLA) models enable generalist robotic manipulation but suffer from high inference latency. This bottleneck stems from the massive number of visual tokens processed by large language backbones. Existing methods either prune or merge tokens uniformly, degrading the spatial reasoning essential for robotic control. We present DepthCache, a training-free framework that leverages depth as a structural prior for visual token compression. It partitions observations into depth-based regions and applies spatially differentiated merge ratios, preserving the near-field workspace while compressing the distant background. To exploit temporal redundancy, DepthCache distributes the merging process across consecutive frames, ensuring consistent representations while reducing per-step computation. A motion-adaptive pipeline further optimizes auxiliary view compression based on end-effector dynamics. The framework requires no model modification, generalizing across diverse VLA architectures. On the LIBERO benchmark, DepthCache achieves up to 1.28x inference speedup with less than 1% average success rate degradation across three VLA models (pi_0.5, OpenVLA, GR00T), whereas pruning and merging baselines incur 4--24% degradation at comparable compression. Real-world experiments on a physical manipulator demonstrate that DepthCache enables faster task throughput and more responsive closed-loop control in latency-sensitive scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型在机器人控制中面临严重的**推理延迟**瓶颈。这种延迟主要源于大型语言模型（LLM）骨干网络需要处理由多视图相机产生的大量视觉 Token。
*   **研究动机**：现有的 Token 缩减技术（如剪枝或均匀合并）存在两个缺陷：
    1.  **空间推理受损**：均匀压缩忽略了机器人操作中“近场工作区需高精度、远场背景可高压缩”的特性，破坏了精细的空间几何关系。
    2.  **缺乏通用性与稳定性**：许多方法需要针对特定架构重新训练，且在连续帧之间缺乏时间一致性，导致机器人动作出现迟疑或抖动。
*   **整体含义**：本文提出了一种受生物视觉启发的、无需训练的框架 **DepthCache**，利用深度信息作为结构先验，实现空间差异化和时间相干的 Token 压缩。

### 2. 方法论
DepthCache 的核心思想是将深度图从感知输入转变为**结构先验**，指导 Token 的差异化合并。
*   **空间差异化合并**：
    *   **双重保护机制**：结合 LLM 的交叉注意力（语义重要性）和深度梯度边缘检测（几何边界），识别并锁定任务关键 Token 不参与压缩。
    *   **深度分区**：对非保护 Token 按深度值进行 K-Means 聚类，划分为 $K$ 个区域。
    *   **差异化比例**：根据区域平均深度分配合并率，远景区域压缩率高，近景区域压缩率低。
*   **时间相干压缩（Progressive Merging）**：
    *   **跨帧分布**：不一次性完成合并，而是将合并过程分散到 $W$ 个连续帧中，利用帧间高度重叠的视觉信息，确保 Token 表示的平滑过渡。
    *   **动态恢复**：监测深度变化，若某区域发生显著运动，立即恢复全分辨率以保证反应灵敏度。
*   **运动自适应流水线**：针对腕部相机，根据末端执行器的运动状态（如快速移动 vs 精细操作）自动切换压缩模式。

### 3. 实验设计
*   **数据集/场景**：
    *   **模拟环境**：使用 **LIBERO** 基准测试，包含 Spatial、Object、Goal、Long 四个子集，共 40 个操作任务。
    *   **真实世界**：使用 PIPER 6-自由度机械臂，执行抓取放置、叠方块、开抽屉等任务。
*   **Benchmark 与对比方法**：
    *   **VLA 基础模型**：$\pi_{0.5}$ (3.3B), OpenVLA (7B), GR00T (2.2B)。
    *   **对比方法**：包括 Token 剪枝方法（FastV, SP-VLA）和 Token 合并方法（ToSA）。

### 4. 资源与算力
*   **硬件环境**：所有模拟和真实世界实验均在单张 **NVIDIA RTX 4090 (24 GB)** GPU 上运行。
*   **训练时长**：该方法为 **Training-free（无需训练）**，因此不存在训练时长。它直接应用于预训练好的 VLA 模型推理阶段，具有极高的部署效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **模拟实验**：在 LIBERO 的 40 个任务上，每个任务评估 100 个回合，总计 4000 次评估，覆盖了三种不同的 VLA 架构。
    *   **消融实验**：详细分析了深度分区、双重保护、渐进合并、重初始化等组件对性能的影响。
    *   **真实实验**：核心任务 60 次试验，扩展场景（多物体排序、扰动恢复）30 次试验。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型（不同参数量和架构）、跨环境（模拟 vs 现实）以及与最新基准方法的对比，有力证明了该方法的泛化能力和鲁棒性。

### 6. 主要结论与发现
*   **性能保持**：在三种 VLA 模型上，DepthCache 实现了 **1.07× 至 1.28×** 的推理加速，而平均成功率（SR）下降**不到 1%**。
*   **优于基准**：相比之下，传统的剪枝和合并方法在同等压缩率下会导致 4% 到 24% 的成功率大幅下降。
*   **实时性提升**：在真实世界中，DepthCache 显著缩短了任务完成时间（提升约 22.7%），并在扰动恢复场景中表现出更快的重规划能力。
*   **空间保留**：实验发现，保留近场几何结构对于防止机器人“动作迟疑”至关重要。

### 7. 优点
*   **无需训练（Plug-and-Play）**：无需修改模型权重或重新训练，可直接适配任何接受视觉 Token 输入的 VLA 模型。
*   **生物启发式设计**：模拟人类视觉的“中央凹-周边”特性，在减少计算量的同时不丢失关键空间上下文。
*   **时间稳定性**：通过渐进式合并解决了 Token 缩减中常见的帧间不一致问题，使机器人动作更流畅。
*   **多视图优化**：针对主视角和腕部视角采取了不同的自适应策略。

### 8. 不足与局限
*   **加速上限受限**：作为纯推理侧优化，它仅加速了视觉 Token 的处理。根据阿姆达尔定律（Amdahl's Law），其总加速比受限于动作解码阶段（如扩散模型去噪步数）的耗时。
*   **传感器依赖**：依赖于深度图的质量。在深度传感器噪声大或无法获取深度的场景下，效果可能会打折扣。
*   **任务覆盖**：虽然在 LIBERO 上表现优异，但在更复杂、物体更密集的极端动态场景下的表现仍有待进一步验证。

（完）
