---
title: "HSC-VLA: Hierarchical Scene-Clearing for Robust Bimanual Manipulation in Dense Clutter"
title_zh: HSC-VLA：用于密集堆放环境下鲁棒双臂操作的分层场景清理框架
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, XinXin Xie, Yitong Liu, Zhongzhu Pu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07484v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: VLA中推理与执行解耦的分层框架
tldr: 针对高密度杂乱环境下的双臂操作难题，本文提出HSC-VLA分层框架。该框架通过“场景清理”机制将高层语义推理与底层运动执行解耦，利用“大脑”模块生成任务相关的掩码以过滤干扰，并由“小脑”模块基于过滤后的视觉信息执行扩散策略。实验证明，该方法在复杂超市货架场景中显著优于端到端模型，展现了极强的鲁棒性和长程任务处理能力。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作模型在密集杂乱环境中容易受到无关视觉干扰，导致感知模糊和长程任务执行失败。
method: 提出一种分层架构，由高层模块生成任务掩码过滤背景干扰，底层扩散策略基于掩码视觉进行高频双臂操作。
result: "在高密度杂乱环境下实现了86.7%的成功率，比最强的端到端基准模型π0高出52.4%，并在长程任务中表现优异。"
conclusion: 通过显式的场景清理抽象实现高低层解耦，能有效提升机器人在复杂、高干扰环境下的操作鲁棒性和故障恢复能力。
---

## 摘要
现代视觉-语言-动作（VLA）模型在高度密集的动作环境中经常面临严重的指令遵循失败问题。在这些环境中，与任务无关的视觉杂乱会分散注意力，破坏定位（grounding），并显著降低复杂长程任务场景下的性能。为了克服单体式端到端架构的表示瓶颈，我们提出了 HSC-VLA，这是一个分层框架，通过显式的场景清理抽象，将高层视觉语义推理与低层高频感觉运动执行解耦。HSC-VLA 采用一个高层“大脑”（Brain）来分解长程任务，并生成特定于任务的场景掩码，在抑制干扰物的同时保留与任务相关的几何信息。过滤后的观测结果随后被传递给低层“小脑”（Cerebellum），这是一个基于扩散的策略，仅利用掩码过滤后的视觉和本体感受来执行双臂操作。在密集堆放的超市货架上进行的广泛实验表明，HSC-VLA 在高密度杂乱环境下实现了 86.7% 的综合成功率，比表现最好的单体基准模型（π0-Full FT，34.3%）高出 52.4%。HSC-VLA 还表现出强大的长程任务性能，在杂物分类和补货任务中分别达到 72% 和 66% 的成功率，证明了其在复杂杂乱操作中强大的鲁棒性和有效的故障恢复能力。

## Abstract
Modern Vision--Language--Action models often suffer from critical instruction-following failures in high-density manipulation environments, where task-irrelevant visual clutter dilutes attention, corrupts grounding, and substantially degrades performance in complex long-horizon scenarios. To overcome the representation bottleneck of monolithic end-to-end architectures, we propose HSC-VLA, a hierarchical framework that decouples high-level visual-semantic reasoning from low-level, high-frequency sensorimotor execution through an explicit scene-clearing abstraction. HSC-VLA employs a high-level Brain to decompose long-horizon tasks and to generate task-specific scene masks that preserve task-relevant geometry while suppressing distractors. The filtered observations are then passed to a low-level Cerebellum, a diffusion-based policy that performs bimanual manipulation using only mask-filtered vision and proprioception. Extensive experiments in densely cluttered supermarket shelves demonstrate that HSC-VLA achieves 86.7\% aggregate success under high-density clutter, surpassing the best monolithic baseline ($π_0$-Full FT at 34.3\%) by 52.4\%. HSC-VLA also exhibits strong long-horizon performance, reaching 72\% on clutter sorting and 66\% on restocking, demonstrating strong robustness and effective failure recovery in complex cluttered manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《HSC-VLA: Hierarchical Scene-Clearing for Robust Bimanual Manipulation in Dense Clutter》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的单体式（Monolithic）视觉-语言-动作（VLA）模型（如 RT-2, $\pi_0$）在处理高密度杂乱环境（如超市货架）时存在“表示瓶颈”。
*   **核心挑战**：
    1.  **注意力稀释**：无关的背景物体会干扰模型对目标物体的定位，导致感知模糊。
    2.  **长程任务困境**：高层语义推理与低层高频运动控制在单一模型中难以平衡，容易导致因果混淆和规划失败。
*   **整体含义**：本文提出了一种分层框架 HSC-VLA，通过显式的“场景清理”（Scene-Clearing）机制，将复杂的环境简化为仅包含任务相关几何信息的表示，从而提升机器人在极端干扰下的操作鲁棒性。

### 2. 方法论
HSC-VLA 模仿生物结构，将系统分为“大脑”和“小脑”两部分：
*   **大脑（Brain - 高层规划）**：
    *   使用预训练的大型视觉语言模型（Qwen3-v1-235B）作为推理核心。
    *   **任务分解**：将长程指令分解为子目标序列。
    *   **干扰识别**：识别并生成任务无关区域的边界框（Bounding Boxes）。
*   **场景清理（感知抽象层）**：
    *   利用 SAM 或 Cutie 等分割模型，根据边界框生成像素级掩码（Mask）。
    *   通过掩码过滤原始图像，抑制干扰物，仅保留任务相关的几何结构。
*   **小脑（Cerebellum - 低层执行）**：
    *   采用基于**扩散策略（Diffusion Policy）**的动作生成模型。
    *   **输入**：过滤后的图像 + 机器人本体感受（14维向量）。
    *   **输出**：动作块（Action Chunk），包含双臂 6-DoF 关节增量及夹持器状态。
*   **感知-动作一致性协议**：在离线训练和在线推理中采用完全相同的掩码过滤流程，确保模型不会因视觉分布偏移而失效。

### 3. 实验设计
*   **实验场景**：
    *   **真实环境**：模拟超市货架，每层放置 5-20 个各类商品（SKU），存在严重遮挡和反光。
    *   **仿真环境**：使用 RoboTwin 2.0 物理引擎进行原子技能评估。
*   **任务类型**：抓取（Grasp）、放置（Place）、双臂协作（Bimanual）、杂物分类（Sorting）及补货（Restocking）。
*   **对比基准（Benchmarks）**：
    *   **模仿学习/扩散模型**：ACT, DP, DP3。
    *   **主流 VLA 模型**：RDT-1B, $\pi_0$（包括 LoRA 微调和全参数微调版本）。

### 4. 资源与算力
*   **训练细节**：Batch size 为 32，训练 50k 个梯度步数。使用 AdamW 优化器，学习率从 $5 \times 10^{-5}$ 线性预热后余弦衰减至 $2.5 \times 10^{-6}$。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量。但考虑到使用了 235B 参数量的 Qwen3 模型作为高层推理机，且低层涉及扩散模型训练，推测需要高性能计算集群（如 A100/H100 级别）。

### 5. 实验数量与充分性
*   **数据规模**：采集了 2100 条专家演示轨迹（通过动觉教学获得）。
*   **实验组数**：
    *   在不同密度（低/高）下进行了数百次测试（SR@300, SR@200 等）。
    *   进行了针对“场景简化策略”的消融实验（无掩码 vs 静态掩码 vs 动态清理）。
    *   进行了长程任务（步骤 $\ge 3$ 或 $5$）的鲁棒性验证。
*   **评价**：实验设计较为充分，涵盖了从单步技能到复杂长程任务的完整链条，对比了当前最先进的单体 VLA 模型，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在高密度杂乱环境下，HSC-VLA 的综合成功率达到 **86.7%**，而最强的单体基准模型 $\pi_0$-Full FT 仅为 **34.3%**，性能提升了 **52.4%**。
*   **长程稳定性**：在补货任务中，HSC-VLA 成功率（66%）远超基准模型（14%），证明分层架构能有效抑制误差累积。
*   **动态清理的必要性**：消融实验显示，动态更新掩码对于长程任务至关重要，因为固定掩码无法适应物体移动后的场景变化。

### 7. 优点与亮点
*   **解耦设计**：成功解决了端到端模型在复杂视觉背景下的“注意力稀释”问题。
*   **即插即用**：高层“大脑”使用冻结权重的 VLM，无需重新训练即可利用其强大的语义常识。
*   **鲁棒性强**：通过显式的几何抽象，使底层策略对光照、背景变化和无关物体干扰具有极强的免疫力。

### 8. 不足与局限
*   **依赖性风险**：系统的成功高度依赖于高层 VLM 对干扰物定位的准确性以及分割模型的质量。
*   **计算延迟**：动态掩码更新和高层 VLM 推理会引入额外的计算开销，可能限制机器人在极高频交互场景下的表现。
*   **技能受限**：目前的“工具库”由预定义的原子技能组成，对于需要全新操作原语或极复杂接触动力学的任务，泛化能力可能受限。

（完）
