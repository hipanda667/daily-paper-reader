---
title: "$Δ$VLA: Prior-Guided Vision-Language-Action Models via World Knowledge Variation"
title_zh: $Δ$VLA：基于世界知识变化的先验引导视觉-语言-动作模型
authors: "Yijie Zhu, Jie He, Rui Shao, Kaishen Yuan, Tao Tan, Xiaochen Yuan, Zitong Yu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08361v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 为动作生成建模世界知识变化的先验引导框架
tldr: 针对现有视觉-语言-动作（VLA）模型侧重于预测未来状态而非推理变化过程的问题，本文提出了ΔVLA框架。该框架通过建模相对于当前世界知识先验的变化（Variation）来指导动作生成。核心贡献包括：利用PWKE提取关键环境特征，通过LWVQ在离散潜空间编码知识演变，以及设计CV-Atten减少干扰。实验证明该方法在模拟和真实机器人任务中均达到SOTA水平，且效率显著提升。
selection_source: fresh_fetch
motivation: 现有模型倾向于预测绝对的未来状态，缺乏对动作引起的知识变化过程的深入推理。
method: 提出了包含PWKE知识提取器、LWVQ潜变量量化和CV-Atten条件注意力机制的ΔVLA框架，通过建模世界知识的增量变化来生成动作。
result: 在多个模拟基准测试和真实机器人任务中取得了领先的性能，并展现出更高的推理效率。
conclusion: 通过建模世界知识的变化而非绝对状态，可以更有效地提升VLA模型在复杂机器人操控任务中的感知与控制能力。
---

## 摘要
最近的视觉-语言-动作（VLA）模型通过统一感知、推理和控制，显著推进了机器人操纵。为了实现这种集成，最近的研究采用了一种预测范式，通过建模未来的视觉状态或世界知识来引导动作生成。然而，这些模型强调预测结果，而不是推理变化的潜在过程，而这对于确定如何行动至变重要。为了解决这个问题，我们提出了 $Δ$VLA，这是一个先验引导的框架，它通过建模相对于显式当前世界知识先验的世界知识变化来生成动作，而不是回归绝对的未来世界状态。具体而言：1) 为了构建当前世界知识先验，我们提出了先验引导的世界知识提取器（PWKE），它在辅助头和先验伪标签的引导下，从视觉输入中提取可操纵区域、空间关系和语义线索，从而减少冗余；2) 在此基础上，为了表示世界知识在动作下如何演变，我们引入了潜世界变化量化（LWVQ），通过 VQ-VAE 目标学习离散潜空间来编码世界知识变化，将预测从全模态转向紧凑的潜空间；3) 此外，为了减轻变化建模过程中的干扰，我们设计了条件变化注意力（CV-Atten），促进了解耦学习并保持了知识表示的独立性。在模拟基准和真实机器人任务上的广泛实验表明，$Δ$VLA 在提高效率的同时达到了最先进的性能。代码和真实世界执行视频见 https://github.com/JiuTian-VL/DeltaVLA。

## Abstract
Recent vision-language-action (VLA) models have significantly advanced robotic manipulation by unifying perception, reasoning, and control. To achieve such integration, recent studies adopt a predictive paradigm that models future visual states or world knowledge to guide action generation. However, these models emphasize forecasting outcomes rather than reasoning about the underlying process of change, which is essential for determining how to act. To address this, we propose $Δ$VLA, a prior-guided framework that models world-knowledge variations relative to an explicit current-world knowledge prior for action generation, rather than regressing absolute future world states. Specifically, 1) to construct the current world knowledge prior, we propose the Prior-Guided WorldKnowledge Extractor (PWKE). It extracts manipulable regions, spatial relations, and semantic cues from the visual input, guided by auxiliary heads and prior pseudo labels, thus reducing redundancy. 2) Building upon this, to represent how world knowledge evolves under actions, we introduce the Latent World Variation Quantization (LWVQ). It learns a discrete latent space via a VQ-VAE objective to encode world knowledge variations, shifting prediction from full modalities to compact latent. 3)Moreover, to mitigate interference during variation modeling, we design the Conditional Variation Attention (CV-Atten), whichpromotes disentangled learning and preserves the independence of knowledge representations. Extensive experiments on both simulated benchmarks and real-world robotic tasks demonstrate $Δ$VLA achieves state-of-the-art performance while improving efficiency. Code and real-world execution videos are available at https://github.com/JiuTian-VL/DeltaVLA.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **$\Delta$VLA** 的新型视觉-语言-动作（VLA）模型框架，旨在通过建模“世界知识的变化”而非“绝对的未来状态”来提升机器人操纵的精准度与泛化能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的预测型 VLA 模型（如预测未来图像或世界状态）往往侧重于“未来看起来像什么”，而忽略了“动作如何引起环境变化”的因果推理过程。
*   **研究动机**：
    1.  **缺乏因果锚点**：直接预测未来状态而不参考当前状态，容易导致模型产生脱离现实的“幻觉”。
    2.  **连续变化的波动性**：环境的连续变化受场景和指令影响极大，难以作为稳定的策略调节接口。
    3.  **冗余信息干扰**：全模态预测包含大量与控制无关的背景信息，降低了学习效率。

### 2. 方法论
$\Delta$VLA 的核心思想是将预测任务从“回归绝对状态”转变为“建模相对于当前先验的离散变化”。
*   **PWKE（先验引导的世界知识提取器）**：
    *   利用 **SigLIP**（语义理解）和 **DINOv2**（空间几何）的互补优势。
    *   通过辅助头（Auxiliary Heads）和伪标签（来自 SAM、Depth-Anything v2 和运动掩码）监督提取：**可操纵区域、深度线索和语义特征**。
    *   构建显式的“当前世界知识先验”作为推理的锚点。
*   **LWVQ（潜世界变化量化）**：
    *   采用 **VQ-VAE** 架构，在无监督下学习一个离散的潜空间（Codebook）。
    *   将连续的环境变化编码为紧凑的离散“变化标记（Variation Tokens）”，使策略学习更稳定。
*   **CV-Atten（条件变化注意力机制）**：
    *   设计了一种结构化的注意力掩码，强制每个变化标记仅关注其对应的先验模态（如深度变化关注深度先验）。
    *   减少了跨模态干扰，促进了特征的解耦学习。

### 3. 实验设计
*   **模拟基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务。
    *   **RoboTwin 2.0**：包含 8 个复杂的双臂操纵任务（如叠碗、开笔记本等）。
*   **真实世界场景**：
    *   使用 **Galaxea R1 Lite** 和 **AgileX Cobot Magic** 两个硬件平台。
    *   执行长程任务：抽屉操作、鞋子对齐、折叠 T 恤、盘子水果整理。
*   **对比方法**：包括通用 VLA（OpenVLA, $\pi_0$）、预测型 VLA（CoT-VLA, Seer, DreamVLA）以及扩散策略（Diffusion Policy）等。

### 4. 资源与算力
*   **训练算力**：使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **推理算力**：在单张 **RTX 5090 (32GB) GPU** 上进行实时推理。
*   **训练时长**：论文提到 $\Delta$VLA 的训练效率较高，每 10k 步的训练成本约为 **4.9 小时**（显著低于 OpenVLA 的 11.7 小时和 DreamVLA 的 14.5 小时）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   模拟环境：LIBERO 每个任务评估 50 次，RoboTwin 每个任务评估 100 次。
    *   消融实验：针对 PWKE 组件、LWVQ 表示形式、CV-Atten 有效性、Token 数量、伪标签噪声鲁棒性等做了 6 组以上的详细消融分析。
*   **充分性评价**：实验覆盖了从单臂到双臂、从模拟到真实、从短程到长程的多种维度。引入了噪声鲁棒性测试，证明了模型对不完美伪标签的容忍度，实验设计客观且公平。

### 6. 主要结论与发现
*   **变化建模优于状态预测**：建模 $\Delta W$（变化）比直接预测 $W_{t+n}$（未来状态）能提供更强的因果引导，显著提升成功率。
*   **离散化是关键**：通过 LWVQ 将变化量化为离散 Token，比连续向量更能稳定策略的条件输入。
*   **效率提升**：由于 PWKE 过滤了冗余视觉信息，$\Delta$VLA 在推理延迟（0.105s）和吞吐量（76.2 Hz）上均优于同类预测模型。

### 7. 优点（亮点）
*   **范式创新**：将 VLA 的预测任务从“生成式”转向“增量推理式”，更符合物理世界的动作逻辑。
*   **高效性**：通过提取关键知识点减少了 LLM 处理的 Token 数量，大幅降低了计算开销。
*   **解耦设计**：CV-Atten 解决了多模态信息混杂的问题，使模型在处理几何敏感任务（如抓取）时更精准。

### 8. 不足与局限
*   **依赖预训练模型**：PWKE 的效果在一定程度上受限于 SigLIP 和 DINOv2 的原始表征能力。
*   **伪标签质量限制**：虽然实验证明了对噪声有一定容忍度，但在极端复杂的遮挡场景下，伪标签（如深度、分割）的失效可能影响先验的构建。
*   **应用限制**：目前主要集中在桌面操纵任务，对于大范围移动机器人或极高动态环境的适用性尚待验证。

（完）
