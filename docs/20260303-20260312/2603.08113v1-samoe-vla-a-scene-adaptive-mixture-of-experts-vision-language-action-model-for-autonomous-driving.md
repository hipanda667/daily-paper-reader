---
title: "SAMoE-VLA: A Scene Adaptive Mixture-of-Experts Vision-Language-Action Model for Autonomous Driving"
title_zh: SAMoE-VLA：一种用于自动驾驶的场景自适应混合专家视觉-语言-动作模型
authors: "Zihan You, Hongwei Liu, Chenxu Dang, Zhe Wang, Sining Ang, Aoqi Wang, Yan Wang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08113v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 用于VLA的场景自适应混合专家框架
tldr: 针对自动驾驶中现有VLA模型采用Token级MoE导致决策不稳的问题，本文提出SAMoE-VLA框架。该框架创新性地利用鸟瞰图（BEV）特征引导专家选择，实现场景自适应的专家加权，并引入条件跨模态因果注意力机制整合多模态信息。实验证明其在nuScenes和LangAuto基准上达到SOTA性能，且比同类模型参数更少、效率更高。
selection_source: fresh_fetch
motivation: 传统的Token级MoE机制在自动驾驶中存在专家分工与场景决策失配的问题，导致模型表现不稳定且存在安全隐患。
method: 通过BEV特征提取场景上下文作为MoE路由信号，并采用条件跨模态因果注意力机制统一推理世界状态、语言和动作。
result: 在nuScenes开环和LangAuto闭环测试中均刷新了SOTA记录，性能优于现有的VLA和世界模型方案。
conclusion: 将MoE路由与结构化场景表示相结合，能显著增强VLA模型在复杂驾驶任务中的适应性和推理一致性。
---

## 摘要
视觉-语言-动作（VLA）模型的最新进展通过利用大语言模型（LLM）的理解和推理优势，在自动驾驶领域展现出巨大潜力。然而，我们的实证分析表明，直接将继承自 LLM 架构的现有 Token 级混合专家（MoE）机制应用于 VLA 模型，会导致自动驾驶性能不稳定和安全性下降，这凸显了基于 Token 的专家专业化与场景级决策之间的不匹配。为解决这一问题，我们提出了 SAMoE-VLA，这是一个场景自适应的视觉-语言-动作框架，它将专家选择建立在结构化场景表示而非 Token 嵌入的基础上。我们的核心思想是从封装了交通场景上下文的鸟瞰图（BEV）特征中导出 MoE 路由信号，从而实现针对不同驾驶条件的场景依赖型专家加权与合并。此外，为了支持跨世界知识、感知、语言和动作的时间一致性推理，我们引入了一种条件跨模态因果注意力机制，将世界状态、语言意图和动作历史整合到一个统一的因果推理过程中。在 nuScenes 开环规划数据集和 LangAuto 闭环基准测试上的大量实验表明，SAMoE-VLA 达到了最先进的性能，以更少的参数超越了以往基于 VLA 和基于世界模型的方法。我们的代码将很快发布。

## Abstract
Recent advances in Vision-Language-Action (VLA) models have shown promising capabilities in autonomous driving by leveraging the understanding and reasoning strengths of Large Language Models(LLMs).However, our empirical analysis reveals that directly applying existing token-level MoE mechanisms--which are inherited from LLM architectures--to VLA models results in unstable performance and safety degradation in autonomous driving, highlighting a misalignment between token-based expert specialization and scene-level decision-making.To address this, we propose SAMoE-VLA, a scene-adaptive Vision-Language-Action framework that conditions expert selection on structured scene representations instead of token embeddings. Our key idea is to derive the MoE routing signal from bird's-eye-view (BEV) features that encapsulates traffic scene context, enabling scenario-dependent expert weighting and merging tailored to distinct driving conditions. Furthermore, to support temporally consistent reasoning across world-knowledge, perception, language, and action, we introduce a Conditional Cross-Modal Causal Attention mechanism that integrates world state, linguistic intent, and action history into a unified causal reasoning process. Extensive experiments on the nuScenes open loop planning dataset and LangAuto closed-loop benchmark demonstrate that SAMoE-VLA achieves state-of-the-art performance, outperforming prior VLA-based and world-model-based approaches with fewer parameters.Our code will be released soon.

---

## 论文详细总结（自动生成）

以下是对论文《SAMoE-VLA: A Scene Adaptive Mixture-of-Experts Vision-Language-Action Model for Autonomous Driving》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在自动驾驶中直接套用大语言模型（LLM）的**Token级混合专家（MoE）机制**，导致决策不稳定和安全性下降。
*   **研究动机**：
    *   **粒度错配**：Token级路由会导致同一场景内的不同Token被分配给不同专家，破坏了驾驶决策所需的全局场景语义一致性和时间因果性。
    *   **安全隐患**：实验发现，Token级稀疏MoE相比稠密模型，碰撞率上升了38.4%。
    *   **缺乏自适应性**：现有的自动驾驶MoE方案往往依赖预定义的专家分工（如左转专家、直行专家），限制了模型对复杂交互场景的泛化能力。

### 2. 方法论：核心思想与关键技术
SAMoE-VLA 提出了一个场景自适应的框架，核心包含以下三个关键技术：

*   **场景自适应混合专家（SA-MoE）**：
    *   **核心思想**：将路由信号从 Token 嵌入转移到**结构化的场景表示（BEV）**上。
    *   **可变形场景编码器（DSE）**：利用轻量级的可变形卷积处理鸟瞰图（BEV）特征，捕捉交通几何和交互结构。
    *   **软加权专家融合**：根据 DSE 提取的场景特征计算专家权重，在推理时动态融合专家参数（Soft-weighting），而非离散选择。这保证了专家专业化与场景级决策的对齐。
*   **条件跨模态因果注意力（CMCA）**：
    *   **核心思想**：统一世界状态、语言意图和动作历史。
    *   **技术细节**：设计了一种非对称注意力掩码，允许动作 Token 访问所有上下文（BEV、语言、自我状态），同时确保动作生成过程在时间上满足因果律，避免未来信息泄露。
*   **功能专家分工**：
    *   **世界-语言专家**：负责多模态处理、指令遵循和未来场景预测（世界模型）。
    *   **规划专家**：采用基于**流匹配（Flow Matching）**的机制生成轨迹，通过 SA-MoE 层增强决策的自适应性。

### 3. 实验设计
*   **数据集与场景**：
    *   **nuScenes**：大规模真实世界数据集，用于**开环规划**评估（L2 误差、碰撞率）。
    *   **LangAuto (CARLA)**：闭环仿真基准，包含 64K 多模态指令-传感器-控制剪辑，评估导航指令遵循能力。
*   **对比方法**：
    *   **传统端到端/世界模型**：UniAD, VAD, Drive-WM, PreWorld 等。
    *   **VLA/VLM 基准**：OpenEMMA (7B), ORION (7B), Omnidrive (7B), EMMA 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 GPU** 进行训练。
*   **模型规模**：总参数量约为 **3.6B**（基于 InternVL2-2B 架构进行扩展）。
*   **训练细节**：分为预训练（世界模型对齐）和最终训练（规划专家微调）两个阶段。使用了全参数微调而非 LoRA，学习率为 1e-4。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的实验，包括：
    *   **主干对比**：在开环和闭环两个主流基准上与十余种 SOTA 方法对比。
    *   **消融实验**：针对 MoE 机制（Token-level vs. Scene-level）、专家数量（2 到 16 个）、DSE 组件（DCN、距离图）、注意力机制等进行了多组对比。
    *   **场景分析**：专门针对复杂路口、窄路转弯、近距离超车等挑战性场景做了性能分解。
*   **充分性评价**：实验设计非常充分且客观。通过 t-SNE 可视化证明了路由信号与交通场景的对应关系，并提供了详细的计算效率和部署分析（见附录），增强了结论的说服力。

### 6. 主要结论与发现
*   **性能领先**：SAMoE-VLA 在 nuScenes 上实现了最低的平均 L2 误差（0.29m）和碰撞率（0.26%），在 LangAuto 闭环测试中 Driving Score（51.4）排名第一。
*   **参数效率**：以 3.6B 的参数量超越了许多 7B 甚至更大规模的模型，证明了 SA-MoE 架构的高效性。
*   **安全性提升**：相比 Token 级 MoE，场景级路由显著降低了长时预测的误差累积和碰撞风险。
*   **路由规律**：t-SNE 分析显示，DSE 能够自动聚类出不同的交通模式，并引导专家进行相应的专业化分工。

### 7. 优点与亮点
*   **创新路由机制**：首次提出基于 BEV 结构化特征的场景级 MoE 路由，解决了 VLA 在驾驶任务中的不稳定性问题。
*   **因果一致性**：CMCA 机制优雅地解决了多模态信息融合与时间因果推理的矛盾。
*   **软融合策略**：采用参数融合而非离散路由，避免了训练不稳和 Token 丢失问题，且推理效率高（仅需执行一次融合后的 FFN）。
*   **轻量化 SOTA**：在保持高性能的同时显著降低了参数门槛，更利于端侧部署。

### 8. 不足与局限
*   **预训练依赖**：模型性能在很大程度上仍依赖于 InternVL2 等强大的预训练视觉语言模型底座。
*   **实时性挑战**：虽然附录分析了推理延迟，但在嵌入式车载芯片上，频繁的参数融合（Soft-weighting）可能仍存在内存带宽瓶颈。
*   **数据偏差**：实验主要集中在 nuScenes 和 CARLA，对于极端天气或罕见长尾场景（如施工区、特殊交通管制）的覆盖仍有待进一步验证。

（完）
