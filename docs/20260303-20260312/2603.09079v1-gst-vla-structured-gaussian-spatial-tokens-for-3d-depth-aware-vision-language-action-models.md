---
title: "GST-VLA: Structured Gaussian Spatial Tokens for 3D Depth-Aware Vision-Language-Action Models"
title_zh: GST-VLA：用于 3D 深度感知视觉-语言-动作模型的结构化高斯空间 Token
authors: "Md Selim Sarowar, Omer Tariq, Sungho Kim"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09079v1"
tags: ["query:vla-ar"]
score: 10.0
evidence: 用于3D深度感知VLA的高斯空间分词器
tldr: 针对现有VLA模型缺乏3D几何结构的问题，本文提出GST-VLA。该模型通过高斯空间分词器（GST）将深度和语义特征转化为128个3D高斯基元，有效捕捉局部表面方向和几何置信度。同时，引入深度感知思维链（DA-CoT）推理空间中间步骤，并结合流匹配动作专家模型。实验表明，GST-VLA在LIBERO和SimplerEnv基准测试中均取得领先性能，显著增强了机器人在精密任务中的空间感知与操作精度。
selection_source: fresh_fetch
motivation: 现有VLA模型将视觉观测编码为缺乏几何结构的2D补丁，难以处理需要精确3D空间理解的复杂操作任务。
method: 提出高斯空间分词器GST构建3D表征，并利用深度感知思维链DA-CoT监督空间推理，配合流匹配动作专家生成高精度动作。
result: "在LIBERO任务中达到96.4%的成功率，在SimplerEnv中达到80.2%，分别比现有SOTA提升了2.0%和5.4%。"
conclusion: 结构化的3D高斯表征与深度感知推理的结合，能显著提升视觉语言动作模型在精密机器人操作中的性能和鲁棒性。
---

## 摘要
VLA 模型将视觉观测编码为不具备内在几何结构的 2D patch token。我们提出了 GST-VLA，包含两项贡献。首先，高斯空间分词器 (GST) 将冻结的稠密深度和冻结的语义 patch 特征转换为 $N_g{=}128$ 个各向异性 3D 高斯原语 (primitives)，每个原语由度量残差均值 $μ\in \mathbb{R}^3$、对数尺度协方差 $\log σ\in \mathbb{R}^3$ 和学习到的不透明度 $α\in (0,1)$ 参数化。协方差特征结构编码了局部表面方向，而不透明度提供了每个原语的几何置信度，这两者都是标量深度无法获取的。带有学习查询的空间注意力池化将固定的 token 预算集中在几何显著区域，而不是均匀分布。其次，3D 深度感知思维链 (DA-CoT) 推理监督了四个结构化的中间空间思维，包括 3D 物体定位、抓取可利用性 (affordance) 接触几何、成对度量距离和粗略的 SE(3) 路径点，作为训练损失中的显式生成目标。在 DA-CoT 生成过程中，每个 VLM Transformer 块中的交叉注意力子层提供了对原始 2

## Abstract
VLA models encode visual observations as 2D patch tokens with no intrinsic geometric structure. We introduce GST-VLA with two contributions. First, the Gaussian Spatial Tokenizer (GST) converts frozen dense depth and frozen semantic patch features into $N_g{=}128$ anisotropic 3D Gaussian primitives, each parameterized by a metric residual mean $μ\in \mathbb{R}^3$, log-scale covariance $\log σ\in \mathbb{R}^3$, and learned opacity $α\in (0,1)$. The covariance eigenstructure encodes local surface orientation, and opacity provides per-primitive geometric confidence, both inaccessible from scalar depth. Spatial attention pooling with learned queries concentrates the fixed token budget on geometrically salient regions rather than distributing uniformly. Second, 3D Depth-Aware Chain-of-Thought (DA-CoT) reasoning supervises four structured intermediate spatial thoughts, covering 3D object grounding, grasp affordance contact geometry, pairwise metric distances, and coarse SE(3) waypoints, as explicit generation targets in the training loss. A cross-attention sublayer at every VLM transformer block provides direct access to the raw 256-primitive Gaussian field during DA-CoT generation. A 300M-parameter flow-matching action expert with mixture-of-experts feedforward sublayers decodes 7-DoF delta action chunks via conditional ODE integration, conditioned on both VLM hidden states and DA-CoT outputs through dual cross-attention. Trained with composite $\mathcal{L}_\mathrm{flow} + \mathcal{L}_\mathrm{CoT} + \mathcal{L}_\mathrm{depth}$ across three progressive stages, GST-VLA achieves 96.4% on LIBERO (+2.0%), and 80.2% on SimplerEnv (+5.4%). Ablations isolate the contribution of each GST component, each DA-CoT thought, and each training stage, confirming independent and synergistic gains concentrated on precision demanding tasks.

---

## 论文详细总结（自动生成）

# GST-VLA: 结构化高斯空间 Token 的 3D 深度感知视觉-语言-动作模型总结

## 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型主要将视觉观测编码为 2D 补丁（patch tokens），这种方式忽略了物理世界内在的 3D 几何结构。即使是引入了深度信息的模型（如 DepthVLA），也仅是将深度视为像素级的标量值，无法捕捉表面的方向（法线）或几何估计的置信度。此外，现有模型在生成动作前缺乏中间的空间推理验证，导致在需要毫米级精度的任务（如边缘抓取、精密插入）中表现欠佳。

**GST-VLA** 的核心动机是通过引入**结构化的 3D 高斯表征**和**深度感知思维链（DA-CoT）**，赋予机器人显式的 3D 空间推理能力，从而提升操作精度和鲁棒性。

## 2. 方法论：核心思想与关键技术
GST-VLA 包含五个顺序阶段，其核心由两个创新组件驱动：

*   **高斯空间分词器 (Gaussian Spatial Tokenizer, GST)**：
    *   **3D 投影**：利用冻结的深度估计器将 2D 语义特征投影到 3D 空间，形成 256 个原始 3D 锚点。
    *   **高斯参数化**：每个 Token 被参数化为一个各向异性的 3D 高斯基元，包含：
        *   **均值 ($\mu$)**：3D 空间位置残差。
        *   **协方差 ($\Sigma$)**：其特征结构编码了局部表面的方向（如平面的法线）。
        *   **不透明度 ($\alpha$)**：代表几何置信度，用于抑制反光或无纹理区域的噪声。
    *   **空间注意力池化**：通过学习到的查询（Queries）将 256 个原始 Token 压缩为 128 个结构化 Token，使模型能将计算资源集中在几何显著区域（如物体边缘）。
*   **深度感知思维链 (DA-CoT)**：
    *   在输出动作前，VLM 必须按顺序生成四个结构化空间思维：**3D 物体定位**（中心点坐标）、**抓取可利用性**（接触点几何）、**度量空间关系**（物体间距离）、**SE(3) 运动规划路径点**。
    *   在推理过程中，VLM 的每个 Transformer 块通过交叉注意力机制直接访问原始的 256 个高斯基元，以获取高分辨率几何细节。
*   **流匹配动作专家 (Flow-Matching Action Expert)**：
    *   一个 300M 参数的 Transformer，采用混合专家（MoE）结构，通过条件常微分方程（ODE）积分生成 7-DoF 的动作序列。

## 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 130 个任务，涵盖空间关系、物体操作、长程任务等。
    *   **SimplerEnv**：基于 BridgeData V2 的视觉匹配仿真环境，测试模型在视觉偏移下的泛化能力。
    *   **LIBERO-Pro**：用于评估精密插入、薄物体抓取和杂乱场景下的性能。
*   **Benchmark 与对比方法**：
    *   对比了 **OpenVLA**、**CogACT**、**SpatialVLA** 和 **$\pi_0$ (DepthVLA)** 等当前最先进的（SOTA）模型。
    *   设置了消融变体：GST-VLA†（仅 GST，无 CoT）和 GST-VLA‡（仅 CoT，使用普通深度 Token）。

## 4. 资源与算力
论文详细记录了三阶段训练的算力消耗：
*   **训练设备**：使用 8 台 **NVIDIA A100-80GB** GPU。
*   **训练时长**：
    *   **第一阶段 (S1)**：GST 与动作专家预训练，80K 步，Batch Size 256。
    *   **第二阶段 (S2)**：LoRA 适配与 DA-CoT 监督，40K 步，Batch Size 128。
    *   **第三阶段 (S3)**：全量微调，20K 步，Batch Size 64。
*   **推理速度**：在单张 A100 上运行频率为 **6.2 Hz**（端到端延迟约 161ms）。

## 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准测试上进行了广泛评估，并针对 GST 的每个组件（如 3D 傅里叶编码、池化方式、各向异性等）和 DA-CoT 的每个思维步骤进行了详尽的消融实验。
*   **客观性与公平性**：
    *   对比实验均在相同的任务集和评估标准下进行。
    *   通过固定 Token 预算（$N_g=128$）对比了不同的 3D 表征（如点云、表面法线、各向同性高斯等），证明了高斯基元的优越性。
    *   实验涵盖了从简单抓取到复杂长程任务的多种维度，结果具有统计学意义。

## 6. 主要结论与发现
*   **性能领先**：GST-VLA 在 LIBERO 上达到 96.4% 的成功率（+2.0%），在 SimplerEnv 上达到 80.2%（+5.4%）。
*   **精密任务优势明显**：在“精密插入”和“薄物体抓取”中提升最显著，证明了表面方向编码和路径点先验的重要性。
*   **协同效应**：GST 提供的结构化几何信息与 DA-CoT 的显式推理具有超加性（Super-additive）增益，两者结合的效果远好于单一组件。
*   **鲁棒性**：3D 投影机制使模型对 2D 像素空间的视觉偏移（如光照变化）更具鲁棒性。

## 7. 优点（亮点）
*   **创新的表征方式**：首次将 3D 高斯原语作为 VLA 模型的空间 Token，有效解决了 2D 视觉缺乏几何结构的问题。
*   **可解释的空间推理**：DA-CoT 不仅提升了性能，还允许开发者通过检查生成的坐标和路径点来诊断模型失败的原因。
*   **高效的训练协议**：三阶段训练策略成功解决了跨模态对齐的难题，确保了 GST 几何校准与 VLM 推理的同步提升。

## 8. 不足与局限
*   **深度估计依赖性**：模型性能高度依赖于预训练深度估计器的准确性。在反光（金属、玻璃）或极端遮挡场景下，深度误差会导致 3D 锚点偏移，进而引发任务失败。
*   **推理开销**：由于引入了 DA-CoT 的自回归生成步骤，推理频率（6.2 Hz）略低于纯 2D 模型（如 OpenVLA 的 9.4 Hz），在极高频控制场景下可能受限。
*   **数据标注成本**：DA-CoT 需要 3D 标注（中心点、接触点等），虽然论文采用了自动标注流程，但在全新环境下的迁移仍需一定的离线处理成本。

（完）
