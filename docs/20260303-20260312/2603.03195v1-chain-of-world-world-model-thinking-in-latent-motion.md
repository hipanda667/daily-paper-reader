---
title: "Chain of World: World Model Thinking in Latent Motion"
title_zh: 世界链：潜空间运动中的世界模型思维
authors: "Fuxiang Yang, Donglin Di, Lulu Tang, Xuancheng Zhang, Lei Fan, Hao Li, Chen Wei, Tonghua Su, Baorui Ma"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03195v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 带有潜运动表示的世界链范式
tldr: 针对现有视觉-语言-动作（VLA）模型在预测背景冗余或缺乏连续动态建模的问题，本文提出CoWVLA。该模型引入“世界链”范式，利用预训练视频VAE将视频分解为结构和运动潜变量。通过预训练推理连续运动链并预测终端帧，并在微调阶段将潜变量动态与离散动作预测对齐。该方法在机器人仿真基准测试中表现优异，实现了高效的视觉运动学习。
selection_source: fresh_fetch
motivation: 现有的世界模型VLA在重建冗余背景上浪费容量，而潜变量动作VLA则缺乏连续的动态建模和世界知识。
method: 提出CoWVLA框架，通过视频VAE提取运动潜变量，并利用统一的自回归解码器联合建模稀疏关键帧与动作序列。
result: 在机器人仿真基准测试中，CoWVLA在性能上超越了现有的世界模型和潜变量动作方法，且具备较高的计算效率。
conclusion: CoWVLA通过结合时间推理与紧凑的潜变量运动表示，为具身智能提供了一种更有效的VLA预训练范式。
---

## 摘要
视觉-语言-动作（VLA）模型是通往具身智能的一条极具前景的路径，但它们往往忽略了视觉动态背后的预测性和时空因果结构。世界模型 VLA 通过预测未来帧来解决这一问题，但在重建冗余背景时浪费了模型容量。潜动作（Latent-action）VLA 紧凑地编码了帧与帧之间的转换，但缺乏时间连续的动态建模和世界知识。为了克服这些局限性，我们引入了 CoWVLA（Chain-of-World VLA），这是一种全新的“世界链”范式，它将世界模型的时间推理与解耦的潜运动表示相结合。首先，预训练的视频 VAE 作为潜运动提取器，将视频片段显式地分解为结构潜变量和运动潜变量。随后，在预训练阶段，VLA 根据指令和初始帧学习推断连续的潜运动链，并预测片段的终止帧。最后，在协同微调阶段，通过在统一的自回归解码器中对稀疏关键帧和动作序列进行联合建模，将这种潜动态与离散动作预测相对齐。该设计保留了世界模型在时间推理和世界知识方面的优势，同时保持了潜动作的紧凑性和可解释性，从而实现了高效的视觉运动学习。在机器人仿真基准测试上的大量实验表明，CoWVLA 优于现有的世界模型和潜动作方法，并实现了适度的计算效率，突显了其作为更有效的 VLA 预训练范式的潜力。项目网站见 https://fx-hit.github.io/cowvla-io。

## Abstract
Vision-Language-Action (VLA) models are a promising path toward embodied intelligence, yet they often overlook the predictive and temporal-causal structure underlying visual dynamics. World-model VLAs address this by predicting future frames, but waste capacity reconstructing redundant backgrounds. Latent-action VLAs encode frame-to-frame transitions compactly, but lack temporally continuous dynamic modeling and world knowledge. To overcome these limitations, we introduce CoWVLA (Chain-of-World VLA), a new "Chain of World" paradigm that unifies world-model temporal reasoning with a disentangled latent motion representation. First, a pretrained video VAE serves as a latent motion extractor, explicitly factorizing video segments into structure and motion latents. Then, during pre-training, the VLA learns from an instruction and an initial frame to infer a continuous latent motion chain and predict the segment's terminal frame. Finally, during co-fine-tuning, this latent dynamic is aligned with discrete action prediction by jointly modeling sparse keyframes and action sequences in a unified autoregressive decoder. This design preserves the world-model benefits of temporal reasoning and world knowledge while retaining the compactness and interpretability of latent actions, enabling efficient visuomotor learning. Extensive experiments on robotic simulation benchmarks show that CoWVLA outperforms existing world-model and latent-action approaches and achieves moderate computational efficiency, highlighting its potential as a more effective VLA pretraining paradigm. The project website can be found at https://fx-hit.github.io/cowvla-io.

---

## 论文详细总结（自动生成）

以下是对论文《Chain of World: World Model Thinking in Latent Motion》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型在具身智能领域虽有进展，但存在两大局限：
    1.  **世界模型 VLA**：通过预测未来完整图像帧来建模环境动态，但这会导致模型浪费大量算力去重建冗余的静态背景（如地板、墙壁），而非关注关键的交互运动。
    2.  **潜动作（Latent Action）VLA**：虽然能紧凑地编码帧间转换，但通常仅限于两帧之间的局部变化，缺乏长时间尺度的连续动态建模和对世界演变知识的理解。
*   **核心目标**：提出一种名为 **CoWVLA** 的新范式，旨在结合世界模型的时间推理能力与潜动作的紧凑性，通过“世界链（Chain of World）”在潜空间中进行连续运动推理。

### 2. 方法论
*   **核心思想**：将视频片段解耦为“结构（Structure）”和“运动（Motion）”潜变量，让 VLA 模型在潜空间中预测运动链，并辅以稀疏关键帧预测来锚定物理世界知识。
*   **关键技术细节**：
    1.  **潜运动提取器（Latent Motion Extractor）**：基于预训练的视频 VAE（VidTwin），通过两个分支将视频片段 $V_{1:f}$ 分解为：
        *   **结构潜变量 $z_s$**：捕获全局语义和静态背景。
        *   **运动潜变量 $z_m$**：通过空间平均池化提取水平和垂直方向的动态特征。
    2.  **VLA 解码器**：采用统一的自回归 Transformer（基于 8.5B 的 Emu3）。
    3.  **两阶段训练流程**：
        *   **预训练阶段**：输入指令和初始帧，利用一个可学习的“运动查询（Motion Query）”令牌 $Q$ 来预测连续的潜运动 $\hat{z}_m$，并重建该片段的终止帧 $v_f$。
        *   **协同微调阶段**：将潜动态推理与离散动作预测对齐。输入序列变为交替的关键帧和动作块，由 $Q$ 聚合时间动态，引导多步动作生成。
*   **公式/流程简述**：
    *   预训练损失：$L_{pretrain} = \|\hat{z}_m - z_m\|^2_2 + \text{CE}(\text{视觉令牌})$。
    *   微调损失：结合了动作预测损失、潜运动一致性损失和稀疏关键帧重建损失。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：LIBERO（包含 Spatial, Object, Goal, Long 四个子集）、SimplerEnv（WidowX 和 Google Robot 臂）、CALVIN（长程任务）。
    *   **真实世界**：使用 Realman RM75B 机械臂进行抓取实验。
*   **Benchmark 与对比方法**：
    *   **VLA 基准**：OpenVLA, SpatialVLA, $\pi_0$, GR00T-N1 等。
    *   **潜动作方法**：LAPA, villa-X, TLA。
    *   **世界模型方法**：WorldVLA, UniVLA, FlowVLA。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **VAE 微调**：4 张 A800 GPU。
    *   **VLA 预训练**：32 张 A800 GPU，训练 10k 步，耗时约 24 小时。
    *   **VLA 协同微调**：16 张 A800 GPU，根据任务不同训练 8k-16k 步，每个配置约需 1-2 天。
*   **模型规模**：主干网络为 8.5B 参数的 Emu3。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流仿真基准测试和 1 个真实机器人场景，总计进行了数十组对比实验。
*   **消融实验**：非常充分，详细探讨了：
    *   损失权重（$\lambda_1, \lambda_2$）的影响。
    *   结构潜变量 vs 运动潜变量的有效性。
    *   关键帧数量 $N$ 和动作块长度 $l_a$ 的敏感度分析。
    *   不同视频 VAE（如 Wan 2.1）的替换对比。
*   **客观性**：通过在 SimplerEnv 等具有强真实相关性的基准上测试，并提供了可视化分析（如运动热图、交叉重建），证明了实验结果的客观性和可解释性。

### 6. 主要结论与发现
*   **性能领先**：CoWVLA 在 LIBERO 和 SimplerEnv 上均达到了 SOTA 性能，超越了纯世界模型（如 UniVLA）和纯潜动作方法。
*   **解耦的价值**：显式分离结构和运动能产生更清晰的物理先验，显著提升动作推理的稳定性。
*   **效率平衡**：相比于预测所有中间帧的世界模型，CoWVLA 在保持高性能的同时，显著降低了显存占用并提升了训练速度。
*   **泛化能力**：在跨域任务（如 SimplerEnv）中表现出更强的稳定性。

### 7. 优点
*   **范式创新**：首次提出“世界链”概念，将连续运动推理引入潜空间，解决了背景冗余重建的问题。
*   **可解释性强**：通过解耦表示，可以直观地观察到模型预测的“运动轨迹”与“场景结构”。
*   **高效性**：利用稀疏关键帧和紧凑潜变量，实现了比传统视频预测更高效的预训练路径。

### 8. 不足与局限
*   **依赖性**：潜运动空间的质量高度依赖于预训练视频 VAE 的性能和领域覆盖范围，若 VAE 未见过的环境可能导致特征失效。
*   **计算开销**：虽然比全帧预测高效，但 8.5B 的模型基座仍需要大量的计算资源，难以在低功耗嵌入式设备上直接部署。
*   **任务复杂度**：真实机器人实验（抓取杯子）相对简单，尚未在极其复杂的非结构化真实环境中进行大规模验证。

（完）
