---
title: "NS-VLA: Towards Neuro-Symbolic Vision-Language-Action Models"
title_zh: NS-VLA：迈向神经符号视觉-语言-动作模型
authors: "Ziyue Zhu, Shangyang Wu, Shuai Zhao, Zhiqiu Zhao, Shengjie Li, Yi Wang, Fang Li, Haoran Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09542v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 具有符号编码器的神经符号视觉-语言-动作框架
tldr: 本研究针对视觉-语言-动作（VLA）模型在数据效率、原语重用和探索能力上的不足，提出了NS-VLA框架。该框架通过神经符号方法，利用符号编码器提取结构化特征，并结合符号求解器实现高效动作序列生成。此外，引入在线强化学习以超越演示数据进行自主探索。实验证明，NS-VLA在单样本学习、鲁棒性和零样本泛化方面表现卓越，为构建高效且通用的机器人控制模型提供了新路径。
selection_source: fresh_fetch
motivation: 现有VLA模型在学习可重用原语、降低对大规模数据的依赖以及实现超越演示的自主探索方面面临挑战。
method: 提出一种神经符号框架，结合符号编码器提取结构化原语、符号求解器进行动作排序，并利用在线强化学习优化生成过程。
result: 实验显示NS-VLA在单样本训练、抗数据扰动和零样本泛化能力上均优于此前方法，且具有极高的数据效率。
conclusion: 神经符号架构与在线强化学习的结合有效提升了VLA模型的泛化性、鲁棒性及在复杂环境下的探索空间。
---

## 摘要
视觉-语言-动作（VLA）模型旨在将指令与视觉上下文关联，并为机器人操作生成动作序列。尽管近期取得了进展，VLA 模型在学习相关且可重用的原语、减少对大规模数据和复杂架构的依赖，以及实现超越演示的探索方面仍面临挑战。为了应对这些挑战，我们提出了一种通过在线强化学习（RL）实现的创新神经符号视觉-语言-动作（NS-VLA）框架。该框架引入了一个符号编码器来嵌入视觉和语言特征并提取结构化原语，利用符号求解器进行高效的数据动作排序，并利用在线强化学习通过广泛探索来优化生成。在机器人操作基准测试上的实验表明，NS-VLA 在单样本训练和数据扰动设置下均优于以往的方法，同时展现出卓越的零样本泛化能力、高数据效率和扩展的探索空间。我们的代码已开源。

## Abstract
Vision-Language-Action (VLA) models are formulated to ground instructions in visual context and generate action sequences for robotic manipulation. Despite recent progress, VLA models still face challenges in learning related and reusable primitives, reducing reliance on large-scale data and complex architectures, and enabling exploration beyond demonstrations. To address these challenges, we propose a novel Neuro-Symbolic Vision-Language-Action (NS-VLA) framework via online reinforcement learning (RL). It introduces a symbolic encoder to embedding vision and language features and extract structured primitives, utilizes a symbolic solver for data-efficient action sequencing, and leverages online RL to optimize generation via expansive exploration. Experiments on robotic manipulation benchmarks demonstrate that NS-VLA outperforms previous methods in both one-shot training and data-perturbed settings, while simultaneously exhibiting superior zero-shot generalizability, high data efficiency and expanded exploration space. Our code is available.

---

## 论文详细总结（自动生成）

以下是对论文《NS-VLA: Towards Neuro-Symbolic Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）主要采用端到端的深度学习架构，虽然在模仿学习上取得了进展，但仍面临三大挑战：
*   **缺乏结构感知：** 机器人操作通常由可重用的原语（Primitives）组成，端到端模型直接回归动作序列，难以捕捉任务间的内部逻辑联系，导致泛化性差。
*   **过度依赖大规模数据：** 成功往往建立在复杂架构和海量专家演示的基础上，而为每个任务生成演示在现实中是不切实际的。
*   **环境探索能力有限：** 现有的监督微调（SFT）方法局限于模仿专家轨迹，限制了模型在交互环境中自主探索和自我改进的能力。

**研究动机：** 提出一种神经符号（Neuro-Symbolic）框架，通过将连续的感知与离散的逻辑推理结合，实现结构化、高效且具备探索能力的机器人操作。

### 2. 方法论
NS-VLA 框架由三个紧密耦合的部分组成：

*   **神经符号编码器（Neuro-Symbolic Encoder）：**
    *   **计划生成：** 利用预训练 VLM 将自然语言指令转化为结构化的原语序列（如 `pick(mug) -> place_on(plate)`）。
    *   **符号分类器：** 一个轻量级 MLP，根据当前视觉特征预测正在执行的原语，并引入“单调约束（Monotone Constraint）”确保计划执行的顺序性，防止动作闪烁。
*   **符号求解器（Symbolic Solver）：**
    *   **视觉令牌稀疏化（Visual Token Sparsification）：** 采用查询驱动的 Top-K 注意力机制，仅保留与当前原语相关的视觉特征，大幅降低计算冗余。
    *   **动作生成器：** 使用因果 Transformer 将稀疏视觉上下文、原语嵌入和本体状态映射为连续的动作块（Action Chunks），提高实时控制的稳定性。
*   **在线强化学习优化（Online RL Optimization）：**
    *   **算法：** 采用组相对策略优化（GRPO），通过组内奖励归一化计算优势函数。
    *   **奖励设计：** 引入“原语分段奖励”，包括分段里程碑奖励和基于潜在空间原型的进度塑造奖励（Progress Shaping）。
    *   **KL 锚定：** 在目标函数中加入 KL 散度约束，防止在线优化过程中策略偏离行为克隆（BC）的先验知识。

### 3. 实验设计
*   **数据集与场景：**
    *   **LIBERO：** 包含空间、物体、目标和长程任务的机器人操作基准。
    *   **LIBERO-Plus：** 引入了光照、纹理、布局等 7 维环境扰动的鲁棒性测试集。
    *   **CALVIN：** 用于评估长程任务序列执行和零样本泛化能力。
*   **Benchmark 设置：**
    *   **1-shot 训练：** 每个任务仅提供一个演示轨迹。
    *   **全量数据训练：** 使用完整数据集进行对比。
*   **对比方法：** 包括 OpenVLA, $\pi_0$, UniVLA, VLA-Adapter, ACT, Diffusion Policy 等十余种主流 VLA 模型。

### 4. 资源与算力
*   **论文说明：** 文中提到了使用了不同参数规模的 Qwen3-VL 作为骨干网络（2B, 4B, 8B）。
*   **缺失信息：** 论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模的对比实验，涵盖了三个主流基准测试。
*   **消融实验：** 针对符号分类器（P.C.）、视觉提取器（V.E.）、动作生成器（A.G.）和强化学习（R.L.）分别进行了消融，验证了各组件的必要性。
*   **鲁棒性与泛化性分析：** 专门在 LIBERO-Plus 的扰动环境下和 CALVIN 的跨任务场景下进行了测试。
*   **充分性评价：** 实验设计非常充分且客观。通过 1-shot 实验证明了数据效率，通过扰动实验证明了鲁棒性，通过可视化热力图证明了视觉稀疏化的有效性，实验维度覆盖了 VLA 模型的关键性能指标。

### 6. 主要结论与发现
*   **卓越的性能：** NS-VLA 在 LIBERO 1-shot 设置下达到 69.1% 的成功率，远超 OpenVLA (35.7%)。
*   **极高的数据效率：** 即使在极少数据下，NS-VLA 的性能下降也远小于端到端模型，证明符号计划提供了强大的归纳偏置。
*   **强鲁棒性：** 在环境扰动下，NS-VLA 的性能衰减最小，能够准确捕捉任务原语与场景上下文之间的不变关联。
*   **更广的探索空间：** 在线 RL 使得模型生成的动作轨迹覆盖范围更广，能够通过探索找到比专家演示更优的路径。

### 7. 优点
*   **神经符号结合：** 成功将 VLM 的感知能力与符号逻辑的结构化优势结合，解决了端到端模型“黑盒”执行的问题。
*   **计算高效：** 通过视觉令牌稀疏化和动作块生成，显著降低了推理时的计算开销。
*   **训练稳定：** 提出的 JGRPO 算法结合了 KL 约束和分段奖励，解决了稀疏奖励环境下在线 RL 难以收敛的问题。

### 8. 不足与局限
*   **原语依赖：** 目前仍需要手动定义动作原语（如 pick, place），原语的粒度对性能影响较大。
*   **Sim-to-Real 挑战：** 实验主要在仿真环境中完成，虽然考虑了环境扰动，但在真实物理世界中的部署（如摩擦力、传感器噪声）仍需进一步验证。
*   **可扩展性：** 随着任务复杂度的增加，手动标注原语和设计符号计划的成本可能会增加，未来需要探索原语的自动发现机制。

（完）
