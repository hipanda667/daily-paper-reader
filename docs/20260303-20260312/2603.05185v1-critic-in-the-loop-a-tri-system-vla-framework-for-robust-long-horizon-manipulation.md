---
title: "Critic in the Loop: A Tri-System VLA Framework for Robust Long-Horizon Manipulation"
title_zh: 环路中的评论者：一种用于鲁棒长程操作的三系统 VLA 框架
authors: "Pengfei Yi, Yingjie Ma, Wenjiang Xu, Yanan Hao, Shuai Gan, Wanting Li, Shanlin Zhong"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05185v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 用于VLA的仿生三系统架构
tldr: 针对视觉机器人操作中高层推理与底层控制的平衡难题，本文提出了一种名为“Critic in the Loop”的三系统VLA框架。该框架结合了负责全局推理的VLM、负责快速执行的VLA以及负责动态调度的轻量级视觉Critic。Critic通过实时监控工作空间，在常规任务中维持VLA闭环执行，并在检测到异常时触发VLM重规划，从而在减少昂贵VLM调用的同时，显著提升了长程任务的鲁棒性和自主性。
selection_source: fresh_fetch
motivation: 现有的VLM推理延迟高，而快速VLA模型在复杂长程任务中缺乏语义深度，难以兼顾实时性与鲁棒性。
method: 提出由VLM大脑、VLA小脑和视觉Critic组成的三系统架构，通过Critic动态调度控制权并集成人类启发式规则以打破死循环。
result: 在挑战性的长程操作基准测试中，该方法在显著减少VLM查询次数的同时，实现了最先进的性能表现。
conclusion: 该框架通过视觉引导的动态调度，有效解决了分布外场景下的任务停滞问题，增强了机器人系统的自主操作能力。
---

## 摘要
在视觉机器人操作中，平衡高层语义推理与底层反应式控制仍然是一个核心挑战。虽然视觉语言模型（VLM）擅长认知规划，但其推理延迟阻碍了实时执行。相反，快速的视觉-语言-动作（VLA）模型通常缺乏复杂长程任务所需的语义深度。为了弥补这一差距，我们引入了“环路中的评论者”（Critic in the Loop），这是一种由动态 VLM-专家调度驱动的自适应分层框架。其核心是一个仿生三系统架构，包括用于全局推理的 VLM 大脑、用于反应式执行的 VLA 小脑以及一个轻量级的视觉评论者（Critic）。通过持续监控工作空间，评论者动态地路由控制权。它通过 VLA 维持常规子任务的快速闭环执行，并在检测到任务停滞或失败等执行异常时，自适应地触发 VLM 进行重新规划。此外，我们的架构无缝集成了受人类启发的规则，以直观地打破无限重试循环。这种基于视觉的调度最大限度地减少了昂贵的 VLM 查询，同时显著增强了系统在分布外（OOD）场景中的鲁棒性和自主性。在具有挑战性的长程操作基准测试上的全面实验表明，我们的方法达到了最先进的性能。

## Abstract
Balancing high-level semantic reasoning with low-level reactive control remains a core challenge in visual robotic manipulation. While Vision-Language Models (VLMs) excel at cognitive planning, their inference latency precludes real-time execution. Conversely, fast Vision-Language-Action (VLA) models often lack the semantic depth required for complex, long-horizon tasks. To bridge this gap, we introduce Critic in the Loop, an adaptive hierarchical framework driven by dynamic VLM-Expert scheduling. At its core is a bionic Tri-System architecture comprising a VLM brain for global reasoning, a VLA cerebellum for reactive execution, and a lightweight visual Critic. By continuously monitoring the workspace, the Critic dynamically routes control authority. It sustains rapid closed-loop execution via the VLA for routine subtasks, and adaptively triggers the VLM for replanning upon detecting execution anomalies such as task stagnation or failures. Furthermore, our architecture seamlessly integrates human-inspired rules to intuitively break infinite retry loops. This visually-grounded scheduling minimizes expensive VLM queries, while substantially enhancing system robustness and autonomy in out-of-distribution (OOD) scenarios. Comprehensive experiments on challenging, long-horizon manipulation benchmarks reveal that our approach achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Critic in the Loop** 的三系统视觉-语言-动作（VLA）框架，旨在解决机器人长程操作中高层规划与底层控制之间的协调难题。

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的机器人操作面临“快慢矛盾”。视觉语言模型（VLM）擅长高层逻辑推理但推理极慢，无法实时控制；而视觉-语言-动作（VLA）模型反应快，但在复杂、长程任务中缺乏语义深度，且在遇到分布外（OOD）异常时容易陷入死循环。
*   **核心目标**：模仿人类大脑（规划）、小脑（执行）和感知反馈（评价）的协作机制，构建一个既能处理复杂逻辑又能实时响应物理变化的鲁棒系统。

### 2. 方法论
论文提出了一个**三系统（Tri-System）架构**：
*   **系统二（大脑 - VLM Brain）**：基于 PaliGemma 等大模型，负责全局推理。它接收用户指令和短期记忆，生成语义子任务（如“抓取蓝色杯子”）。它仅在必要时被唤醒，以避开推理延迟瓶颈。
*   **系统一（小脑 - VLA Cerebellum）**：基于流匹配（Flow Matching）的动作专家。它根据大脑给出的子任务标签，高频生成连续的动作块（Action Chunks），实现实时闭环控制。
*   **系统三（评论者 - Critic）**：核心创新点。这是一个轻量级的视觉模型（如 0.2B 的 Florence-2），以 20Hz 的频率异步监控工作空间。
    *   **进度评估**：通过蒙特卡洛价值估计，将子任务进度量化为 [-1.0, 0.0] 的数值。
    *   **异常检测**：训练模型输出特定的语义标记 `<aci>`（事故发生），用于识别物体掉落或环境干扰。
    *   **动态调度算法**：当 Critic 检测到子任务完成、发生意外或**执行停滞**（长时间进度不更新）时，会强制中断系统一，触发系统二重新规划，并结合人类启发式规则（如姿态重置）打破死循环。
*   **自动化标注管线**：利用 Ramer-Douglas-Peucker (RDP) 几何算法提取关键帧，并结合大模型（Qwen-VL）自动生成子任务标签，解决了长程任务数据标注昂贵的难题。

### 3. 实验设计
*   **硬件平台**：Cobot Magic ALOHA 双臂机器人（14 自由度）。
*   **实验场景**：
    1.  **餐具整理（Arrange Tableware）**：包括顺序摆放、乱序混合、分布外（OOD）左侧杯子抓取、人为干扰（击倒杯子）四种情况。
    2.  **桌面清理（Tidy up the Desk）**：涉及**柔性物体**操作，如撑开塑料袋、装入瓶子和纸巾。
*   **对比基准（Benchmarks）**：
    *   **Single-System $\pi_{0.5}$**：直接从观察输出动作的单系统模型。
    *   **Dual-System $\pi_{0.5}$**：固定频率切换子任务的双系统模型（无 Critic）。

### 4. 资源与算力
*   **模型规模**：大脑/小脑基于 $\pi_{0.5}$（约 3B 参数），Critic 使用 Florence-2-base（约 0.2B 参数）。
*   **训练细节**：使用 AdamW 优化器，学习率 $1 \times 10^{-6}$，在 GPU 上训练 50 个 epoch，batch size 为 64。
*   **算力说明**：文中提到了“per GPU”的配置，但未明确给出具体的 GPU 型号（如 A100 或 H100）及总数。

### 5. 实验数量与充分性
*   **实验规模**：每个场景进行 10 次真实世界测试，涵盖了从简单到复杂、从分布内到分布外的多种变体。
*   **消融实验**：针对 OOD 场景设计了 4 组消融实验，分析了提示词格式、数据分布和人类规则对成功率的影响。
*   **充分性评价**：实验设计较为全面，特别是引入了人为干扰和柔性物体，验证了系统在动态环境下的鲁棒性。对比实验客观地展示了三系统在减少延迟和防止任务停滞方面的优势。

### 6. 主要结论与发现
*   **性能领先**：三系统框架在所有任务中均显著优于单系统和双系统基线。在复杂的“桌面清理”任务中，整体成功率从 0% 提升至 40%。
*   **OOD 鲁棒性**：通过 Critic 触发的“停滞检测”和“机器人重置”规则，系统能有效处理训练数据中未出现的场景（如用左臂处理原本只用右臂训练的任务）。
*   **效率提升**：异步调度机制使得昂贵的 VLM 仅在关键节点调用，保证了 20Hz 的实时控制频率。

### 7. 优点
*   **架构优雅**：通过引入第三系统（Critic）解耦了“思考”与“行动”，解决了 VLM 部署在机器人上的实时性难题。
*   **闭环纠错**：具备主动异常检测能力，不再是盲目执行预设序列，而是根据物理反馈动态调整。
*   **实用性强**：自动化标注管线极大降低了长程任务的学习门槛。

### 8. 不足与局限
*   **大脑瓶颈**：消融实验显示，高层 VLM（系统二）仍存在分布外过拟合问题，有时会给出错误的指令，限制了系统的上限。
*   **样本量限制**：虽然在机器人领域 10 次测试是标准做法，但对于统计显著性而言，样本量仍较小。
*   **依赖专家数据**：目前仍依赖高质量的人类演示数据，未来需要探索如何通过强化学习或世界模型进一步脱离对人工数据的依赖。

（完）
