---
title: "DiT4DiT: Jointly Modeling Video Dynamics and Actions for Generalizable Robot Control"
title_zh: DiT4DiT：联合建模视频动力学与动作以实现可泛化的机器人控制
authors: "Teli Ma, Jia Zheng, Zifan Wang, Chuili Jiang, Andy Cui, Junwei Liang, Shuo Yang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10448v1"
tags: ["query:vla-ar"]
score: 9.0
evidence: 在统一框架中耦合视频扩散Transformer与动作扩散Transformer
tldr: DiT4DiT 是一种端到端的视频-动作模型，通过将视频扩散 Transformer 与动作扩散 Transformer 耦合，解决了现有机器人模型缺乏物理动力学理解的问题。它利用视频生成过程中的中间去噪特征作为动作预测的条件，并采用双流匹配目标进行联合训练。该模型在多个基准测试中达到 SOTA 性能，显著提升了样本效率和收敛速度，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作模型多依赖静态图像预训练，缺乏对物理动力学和时空结构的深入理解。
method: 提出一种级联框架，将视频生成过程中的中间去噪特征提取出来作为动作扩散 Transformer 的时空条件，并使用双流匹配目标进行联合训练。
result: 在 LIBERO 和 RoboCasa 等基准测试中取得 SOTA 成绩，样本效率提升 10 倍以上，收敛速度加快 7 倍，并在真实机器人上实现强泛化。
conclusion: 视频生成可以作为机器人策略学习的有效扩展代理，通过联合建模视频动力学与动作，显著增强了模型的泛化性和学习效率。
---

## 摘要
视觉-语言-动作（VLA）模型已成为机器人学习中一种极具前景的范式，但其表示在很大程度上仍继承自静态图像-文本预训练，导致物理动力学只能从相对有限的动作数据中学习。相比之下，生成式视频模型编码了丰富的时空结构和隐式物理规律，使其成为机器人操控的有力基础。然而，现有文献尚未充分探索其潜力。为了弥补这一差距，我们推出了 DiT4DiT，这是一种端到端的视频-动作模型，它在一个统一的级联框架中将视频扩散 Transformer（Diffusion Transformer）与动作扩散 Transformer 耦合在一起。DiT4DiT 并不依赖于重建的未来帧，而是从视频生成过程中提取中间去噪特征，并将其作为动作预测的时间对齐条件。我们进一步提出了一种具有解耦时间步和噪声尺度的双流匹配（dual flow-matching）目标，用于视频预测、隐藏状态提取和动作推理，从而实现两个模块的连贯联合训练。在仿真和真实世界基准测试中，DiT4DiT 取得了最先进的结果，在 LIBERO 上达到了 98.6% 的平均成功率，在 RoboCasa GR1 上达到了 50.8%，且使用的训练数据大幅减少。在宇树（Unitree）G1 机器人上，它也表现出了卓越的真实世界性能和强大的零样本泛化能力。重要的是，DiT4DiT 将样本效率提高了 10 倍以上，并将收敛速度加快了多达 7 倍，证明了视频生成可以作为机器人策略学习的有效扩展代理（scaling proxy）。我们在 https://dit4dit.github.io/ 发布了代码和模型。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for robot learning, but their representations are still largely inherited from static image-text pretraining, leaving physical dynamics to be learned from comparatively limited action data. Generative video models, by contrast, encode rich spatiotemporal structure and implicit physics, making them a compelling foundation for robotic manipulation. But their potentials are not fully explored in the literature. To bridge the gap, we introduce DiT4DiT, an end-to-end Video-Action Model that couples a video Diffusion Transformer with an action Diffusion Transformer in a unified cascaded framework. Instead of relying on reconstructed future frames, DiT4DiT extracts intermediate denoising features from the video generation process and uses them as temporally grounded conditions for action prediction. We further propose a dual flow-matching objective with decoupled timesteps and noise scales for video prediction, hidden-state extraction, and action inference, enabling coherent joint training of both modules. Across simulation and real-world benchmarks, DiT4DiT achieves state-of-the-art results, reaching average success rates of 98.6% on LIBERO and 50.8% on RoboCasa GR1 while using substantially less training data. On the Unitree G1 robot, it also delivers superior real-world performance and strong zero-shot generalization. Importantly, DiT4DiT improves sample efficiency by over 10x and speeds up convergence by up to 7x, demonstrating that video generation can serve as an effective scaling proxy for robot policy learning. We release code and models at https://dit4dit.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《DiT4DiT: Jointly Modeling Video Dynamics and Actions for Generalizable Robot Control》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型大多基于静态图像-文本预训练（如 GPT-4V 或 Qwen-VL 衍生模型），这导致模型缺乏对物理世界连续动力学、时空结构和因果关系的理解。
*   **研究动机**：视频生成模型（VGM）通过预测未来帧，天然地学习了丰富的物理先验。论文旨在探索：**视频生成能否作为机器人策略学习的高效“扩展代理”（Scaling Proxy）？** 以及如何将视频模型的时空表示与动作生成进行端到端的高效耦合？
*   **整体含义**：提出了一种名为 **DiT4DiT** 的端到端框架，证明了联合建模视频动力学与动作预测比单纯依赖语义对齐（如物体检测或特征对齐）具有更高的样本效率和泛化能力。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将视频扩散 Transformer（Video DiT）作为特征提取器，其生成的中间去噪特征（而非最终像素）作为动作扩散 Transformer（Action DiT）的条件输入。
*   **关键技术细节**：
    *   **双 DiT 级联架构**：由一个预训练的视频生成模型（如 Cosmos-Predict）和一个动作生成头组成。
    *   **中间特征提取（Hidden-state Extraction）**：在视频去噪过程中，通过 Hook 机制拦截特定层（实验证明第 18 层最佳）的隐藏状态，将其作为时空对齐的视觉特征。
    *   **双流匹配（Dual Flow-Matching）**：采用流匹配目标函数同时优化视频和动作生成。
    *   **三时间步方案（Tri-timestep Scheme）**：
        1.  $\tau_v$：视频训练时间步，从均匀分布采样，学习完整的去噪轨迹。
        2.  $\tau_f$：特征提取时间步，固定在特定噪声水平，以提供稳定的视觉条件。
        3.  $\tau_a$：动作训练时间步，采用 Beta 分布采样，专注于关键的控制阶段。
*   **算法流程**：输入当前观测和指令 $\rightarrow$ Video DiT 进行单步前向传播提取特征 $\rightarrow$ Action DiT 结合机器人状态和视觉特征，通过迭代去噪生成动作序列。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO 仿真基准**：包含 Spatial、Object、Goal、Long 四个子集，测试空间泛化、物体泛化、指令理解和长程任务。
    *   **RoboCasa-GR1 Tabletop**：24 个复杂的厨房操控任务，使用 29 自由度的 GR1 人形机器人仿真模型。
    *   **真实世界部署**：在 **Unitree G1 人形机器人**上执行 7 个任务（如叠杯子、插盘子、整理花朵）。
*   **对比方法**：
    *   **SOTA VLA 模型**：$\pi_0$、$\pi_{0.5}$、OpenVLA、CogVLA、UniVLA 等。
    *   **人形机器人基准**：GR00T-N1.5、GR00T-N1.6。
    *   **消融基准**：Qwen3DiT（将视频模型换成参数量相当的 Qwen3-VL 静态模型）。

### 4. 资源与算力
*   **训练算力**：使用了 **32 张 GPU**（型号未直接标明，但根据上下文推测为 A100 级别），最大训练步数为 100,000 步。
*   **部署算力**：真实世界推理在单张 **NVIDIA RTX 4090** 上完成。
*   **模型规模**：可训练参数量约为 **2.2B**。
*   **部署频率**：在 A100 上测试，DiT4DiT 的部署频率为 **6Hz**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 28 个以上的任务场景。
    *   真实世界实验包含 7 个任务，每个任务进行 20 次独立测试。
    *   进行了详尽的**零样本泛化实验**（更换物体类别、颜色、数量）。
*   **充分性与公平性**：
    *   **消融实验**非常深入，探讨了特征提取层数（1-28层）、去噪步数（1-32步）以及联合训练 vs. 解耦训练的影响。
    *   **公平性**：通过构建参数量对齐的 Qwen3DiT 基准，排除了“模型变大导致变强”的干扰，直接证明了视频动力学建模的优越性。

### 6. 主要结论与发现
*   **视频生成是极佳的代理目标**：相比于物体检测（Grounding）或特征对齐（FLARE 风格），视频生成任务能让策略收敛速度快 **7 倍**，样本效率提升 **10 倍**以上。
*   **SOTA 性能**：在 LIBERO（98.6%）和 RoboCasa（50.8%）上均刷新了最高成功率记录。
*   **单步特征即足够**：在推理时，只需进行一次视频去噪前向传播即可获得高质量特征，无需生成完整图像，极大提升了效率。
*   **联合训练的必要性**：联合优化视频和动作模型能显著提高特征的时间连续性（t-SNE 可视化显示其时间流向更清晰）。

### 7. 优点（亮点）
*   **端到端统一性**：将视频生成与动作预测无缝集成在双 DiT 框架下，避免了多阶段训练的误差累积。
*   **极强的泛化性**：在从未见过的物体和环境（如从塑料杯换成玻璃杯）中表现出极高的鲁棒性。
*   **物理感知**：模型表现出对空间约束（如插盘子时的缝隙对齐）的深刻理解，这是传统静态 VLA 难以企及的。

### 8. 不足与局限
*   **推理频率限制**：6Hz 的频率虽然能满足大多数操控任务，但相比于纯动作模型（如 13Hz+）仍有差距，对于极高动态的任务可能存在挑战。
*   **单视角依赖**：目前仅依赖单路第一视角相机，在双臂协作或大物体遮挡时可能存在感知盲区。
*   **计算开销**：尽管做了优化，但运行一个 2B 规模的视频扩散模型作为骨干网，对边缘计算设备的算力仍有一定要求。

（完）
